Build admin 265 / staff 136 — full backlog batch (items 1-5)

B1  noindex meta on both sites (robots.txt can't live under /vacation/)
B2  Mail-queue parking: entries park after 5 failed sends (MQ_MAX_FAILS,
    both sites) instead of retrying forever — ends the corrupted-address
    422 loop. Badge splits "N · M parked"; red flash tracks ACTIVE only;
    new ⛔ Parked dialog (Retry all clears strikes / per-entry delete).
    Rules-verified: the .fails dotted write is single-key, already legal.
B3  Honest labels: "Queued e-mails" / "E-mail Queue" + matching toasts.
B4  EmailJS sent-counter undercount ROOT CAUSE: mailStats removed from
    the full-restore map (restores silently rewound the meter). Backups
    still record it; only the restore skips it.
B5  Auction Calendar config (item 5): year + holiday weeks + summer range
    now live in adminSettings.auctionConfig, editable from a new Controls
    card. Defaults reproduce the old hardcoded 2027 values EXACTLY (absent
    config = behavioral no-op, test-proven). Parse-time localStorage cache;
    authoritative snapshot check reloads ONCE on change (loop-guarded).
    Locked once Phase 1 begins (rehearsal included); unlocks only via
    Reset Auction; values survive reset. ≥1 high-demand holiday enforced.
    Calendar-rule suggestions (MLK/Memorial/Jul4/Labor/Thanksgiving +
    Xmas/NY = last two weeks, per admin's rule); ski/spring stay manual.
    52/53-week years handled (53 ≈ 5-in-28); all "52 weeks" copy dynamic.
    HIGH_DEMAND_WEEKS / SPECIAL caps / SPECIAL_WEEKS / summer windows /
    labels / SIM dates / welcome-e-mail year all derive from one config.

Tests: tests-build265.mjs 25/25 + tests-build265-b5.mjs 46/46 green;
honesty runs (--pre) fail 16 + 19 fix-tests on pristine 264/135.
Diff audit: 42 admin + 12 staff hunks, every one mapped to B1-B5/bumps.
synccheck (node --check): green, all inline scripts, both files.
NOT YET RUN (Mac-only): tests/run-all.mjs, audit-handlers.mjs, Playwright
sweep — REQUIRED before deploy. B5 touches HIGH_DEMAND_WEEKS derivation:
adversarial fairness re-audit REQUIRED before deploy per house rule.
