Build admin 266 / staff 137 — Bid Floors by Week Category

BF  The hardwired "high-demand weeks accept only bids 1-4 + combos" rule
    is now admin-controlled per week category (new 🎚 Bid Floors card in
    Controls, below NP Bids by Phase). ONE RULE: a bid may be placed on a
    week only if it is at least as strong as the category's floor
    (1/2/3 › 1/2 › 1 › 2 › … › 10 › NP — NP is weakest, so any numeric
    floor blocks NP automatically). Categories: High demand (default 4 =
    old behavior) · Summer (default No floor = old behavior) · All other
    weeks always No floor (fixed). Dropdown wording: "No floor — all bids
    allowed". Config: adminSettings.bidFloors (admin-only writable, NO
    rules change). Locked once Phase 1 begins (paint + click-time guard);
    unlocks only via Reset Auction; values survive reset. No cache, no
    reload machinery — floors read live from adminSettings.

    ENGINE-ENFORCED on both twins (closes a pre-existing NE-4 soft spot):
    a below-floor bid, or an NP bid whose PLACEMENT phase had NP toggled
    off, can neither win nor block (NE-1 lockout floor excluded too).
    Legally placed NP bids survive later toggle flips (placement-phase
    rule, owner ruling 13 Aug). Explicit admin approval of a floor-
    filtered bid remains the sanctioned override — honored by the engine,
    warned about in the Approve dialog, never silent. Unscorable / I2 /
    dead prior-phase bids stay refused exactly as before. Simulator,
    rules dialog, and both welcome e-mails derive their wording from the
    floors (default text === the old sentence character-for-character).

Absent config = behavioral no-op, test-proven: tests-build266.mjs 44/44
green incl. full floor×value truth table + cross-site no-op equivalence;
honesty run (--pre) 0 passed / 30 failed on pristine 265/136. Gates:
run-all same-as-baseline signature (never-events 20/20, fairplay 48/48,
engine-fuzz 4/4), audit-handlers 182/0, Playwright sweep 4 passes clean,
dedicated BF-card browser test 10/10 (incl. forced lock-bypass attempt),
full-lifecycle run-through clean (0 errors, 0 NE findings), adversarial
fairness re-audit 11/11 (52 weeks × 4 configs × 13 values cross-site).
