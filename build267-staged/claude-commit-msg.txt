Build admin 267 / staff 138 — calendar redesign (fixed 6 HD weeks) + e-mail link dedup

CAL2 Auction Calendar redesign (owner rulings 13 Aug): the SIX high-demand
     weeks are fixed identity — ⛷️ Ski Week, 🌸 Spring Break ×2,
     🦃 Thanksgiving, 🎄 Christmas, 🎆 New Year's. No HD checkbox, no
     rename/re-icon, no add, no delete; only week placement is chosen.
     ✨ Suggest fills exactly Thanksgiving/Christmas/New Year's; Ski +
     Spring Breaks have NO default, save refuses until all six are set,
     and they clear on year change (computables re-anchor automatically).
     Federal holidays auto-label from the year with zero admin input:
     🕊️ MLK · 🏛️ Presidents · 🇺🇸 Memorial · 🎇 July 4 · 🛠️ Labor.
     One landing ON a chosen HD week (typically Presidents = Ski) rides
     as an informational NOTE — dual label "⛷️ Ski Week · 🏛️ Presidents'
     Day" on both sites, while every rule sees only the HD identity
     (staff 138: normalize preserves note, label renderer appends it).
     Summer "4 default slots" text moved below the week selectors.
     computeSuggestedHolidays byte-identical (b5 pins hold); AC defaults,
     BF core and both engines byte-identical to 266/137 — deploy is a
     behavioral no-op until a calendar is saved (post-reset).

E    Reminder + add-us-to-contacts e-mails no longer embed the site URL
     (the EmailJS template footer carries it); both point at "the site
     link at the bottom of this e-mail". AUCTION_SITE_URL removed.

Gates: tests-build267.mjs 26/26; honesty --pre 24-fail vs pristine
266/137; audit-handlers 180/0; never-events 20/20, fairplay 48/48,
engine-fuzz 4/4, delta 91/91, inflight 17/17; Playwright sweep 4 passes
clean; dedicated editor browser test 13/13 (fixed rows, year-change
clearing, unset refusal, Presidents-note save); BF card regression 10/10.
