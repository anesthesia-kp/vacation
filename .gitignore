# Local-only helper: the pending commit message Claude writes for GitHub
# Desktop to pre-fill. Never committed or pushed.
.claude-commit-msg.txt
.claude-commit-msg-*.txt

# macOS Finder junk — never commit.
.DS_Store

# The commit summary for the build you are about to push. NOT a dotfile on purpose:
# a leading dot makes macOS Finder hide it, and gitignoring it hides it from GitHub
# Desktop too — which made it invisible in both places at once. This name is visible
# in Finder and still never committed.
COMMIT-MESSAGE.txt

# The generated CRNA site — RELEASE GATE. Remove this line (and only then) when the CRNA
# auction goes live: real firebase config in crna-config.json, between-phases window,
# landing page in the same push. Until then the stamped files stay local-only.
crna/
