# BUILD LOG — Vacation Auction (MD)

**Why this file exists.** Same reason as the schedule's: commit subjects have been lost to
mis-pastes before, so every build gets a row here, written at the same time as the code.
Agreed 17 Aug 2026; starts at build 270 (history before 270 lives in
`anesthesia-kp.github.io/HANDOFF.md` and `DECISIONS.md`, both committed).

The build number below is authoritative: read out of `var BUILD` in that commit's own
bytes, not taken from the commit subject.

| build | commit | date | what shipped |
|---|---|---|---|
| **164 (staff) · 303 (admin)** | _pending push_ | 24 Aug 2026 | **BOTH MARKERS ON A SUMMER HOLIDAY WEEK.** Owner-found, looking at the live calendar: *"memorial day is marked as a summer week, but it's missing a summer symbol. why is that?"* **Cause:** a week carries ONE label and a named holiday wins — `SPECIAL_WEEKS[wk] || (isSummer ? {label:'☀️ Summer'} : null)` — so Memorial Day (wk 22, the window's own first week) and July 4th (wk 27) sat inside the summer window showing no sun. **THE FLOOR WAS NEVER WRONG, and that is the important half:** `summerOnly` is derived from the DATE WINDOW, not the label, and admin's `bfFloorFor` does its own date comparison — [153 · AUD-C1] fixed exactly that split. So the risk was never a mis-priced week; it was that a week could be SUBJECT to the summer rule while displaying nothing that said so. With no summer floor currently set there was no live consequence; had one been set before launch, two named holiday weeks would have refused weak bids with no visible reason. **FIX:** new `summerAlso(meta)` — true exactly when the week counts as summer for the floor AND is showing a different label, so the sun means "the summer rule applies here" and nothing looser. Appended at all THREE staff render sites (the suite asserts none was missed) and to admin's dashboard week grid, where high demand keeps precedence, matching `bfFloorFor`. **DELIBERATELY NOT TOUCHED:** the two admin REPORT paths (`weekTypeLabel`, the export tag). Report wording is a separate decision and `weekTypeLabel` carries its own comment about it — flagged to the owner rather than swept in (§85). **GATES:** new `tests/test-staff-164-summer-marker.mjs` — 14 assertions executing the real extracted helper over six week shapes (Memorial Day, July 4th, plain summer, MLK, unlabelled, high-demand Christmas), plus a check that the floor derivation on BOTH sites is untouched. Honesty EXECUTED against the pushed 163 at explicit SHA `87b6cae`: **4 of 14 FAIL there**, exit 1. Battery **54 suites / 2,041 assertions, zero skips** (2,027 + 14, reconciled exactly); isolation 27/27; CRNA restamped. |
| **302 (admin) — SHIPPED INSIDE 303** | `04e00bf` | 24 Aug 2026 | **⚠️ There is no deployed build 302: it was superseded by 303 before the push, so its code went out inside `04e00bf` ("Staff 164, admin 303"). Anyone looking for 302 live will find 303 — the number is a BUILD-LOG entry, not an artifact.** **FAST-1 — FAST DECISIONS: SKIP THE CONFIRM, NEVER THE GUARDS.** Owner: a decision session can be hundreds of clicks, *"I still want to ensure the approvals/denials go through identically whether confirmed through extra pop up dialogue or when that's skipped."* **His three rulings, all built:** skip ONLY a dialog with no warning · available in rehearsal AND live · turning the mode on is itself confirmed by a dialog stating what it does and does not do. **Why this does not breach §3.5:** that rule's own second clause is the licence — *dialogs are paint, GUARDS at the moment of action are the enforcement*. Fast mode removes paint only. **"Identical" is STRUCTURAL, not maintained:** `_fastDecide` calls the very `onConfirm` the dialog would have called, so there is exactly one write path and nothing to drift. **Opt-in per call site** — only `confirmApprove`, `adApprove`, `adDeny`; `openConfirm` itself is untouched, so Reset Auction, Complete Phase, Send Results and the revokes are unaffected. **In memory only**, so a reload turns it off. **The no-warning predicate:** an over-cap overage · a below-floor or NP-off override · denying someone who currently holds an approval · any readiness warning. Any of those and the dialog opens as before — and the "stop asking" offer is not even shown, since it would not apply. **TWO REAL BUGS THE SUITE CAUGHT, both the same shape:** the re-run inside the entry dialog, and the three handlers, each DROPPED the write's promise — so a failed write would have been a silent unhandled rejection, bypassing `openConfirm`'s M-14 net. Both now return it. **AND BUILD 287's OWN GUARD CAUGHT A THIRD:** `_fastOffer` interpolated `${kind}` into an onclick without `_jsq()`. Safe in practice (a literal), fixed anyway — the guard exists so nobody has to reason about it per site. **GATES:** new `tests/test-admin-302-fast.mjs` — 43 assertions, the core one being the owner's: the recorded writes and `adminLog` calls are byte-identical confirmed vs skipped, for all three handlers. Honesty EXECUTED against the pushed 301 at explicit SHA `87b6cae`: **18 of 43 FAIL there**, exit 1, and the suite degrades to clean FAILs rather than a TypeError on a build without the feature. Battery **53 suites / 2,027 assertions, zero skips** (1,984 + 43, reconciled exactly); isolation 27/27; CRNA restamped to 302. Four existing suites needed the new helpers added to their sandboxes. |
| **301 (admin)** | _pending push_ | 24 Aug 2026 | **§88 · SORT-1 — ALWAYS SORT BY PROJECTION.** Owner ruling, verbatim: *"Always sort by projection."* **A decision never drives position; it is shown by the row's badge, in place.** He reached it from two live observations: **approving someone projected to LOSE moved them to the top**, and a **COMPLETED phase was mis-ordered too** — every row there is approved or denied, so bid strength only broke ties inside those two blocks and a strong denied bid sat beneath a weak approved one. Two different symptoms, one cause: position was keyed on the DECISION. **THE RULE, one comparator (`projRank`/`bidRank`) used everywhere:** projection (win → draw/review as ONE tier → lose), then bid strength strongest-first via `pScore`, then name — or the WEEK in by-user views, where every row is the same person. Draw and review share a tier safely because per week they are mutually exclusive: the engine's group loop sets `settled` at the first non-fitting group. **SIX SITES:** both Approvals/Denials views · both Draws & Reviews views · the by-week report · the context rows beneath a week card, which previously sorted by strength alone and so disagreed with the rows above them. **`exportUserSummary` already complied** (per-user, week-ordered) and the best-bids table carries no outcomes — checked, not assumed. **ONE REAL ADDITION:** the panel held NO projection for past-phase rows, so those fell back to NAME order — the completed-phase case the owner reported. It now reconstructs the projection from the phase snapshot via `snapshotProjection`, the same source the report already used, cached per render. **MEASURED FIRST (§82d), answering the owner's own question:** over 6,000 week states `computeApprovals(true)` and `(false)` are **IDENTICAL** and neither moves when a decision is written — §77 hollowed the distinction out, so `ignoreAdmin` is now vestigial and the projection is a stable key by construction. **GATES:** new `tests/test-admin-301-sort.mjs` — 23 assertions, the ordering ones EXECUTING the real render. Its scenario inverts completely between the rules: deny a projected winner, approve a projected loser, and the pushed 300 renders **DEV → BEN → CAI → ANN** where 301 renders **ANN → BEN → CAI → DEV**. Honesty EXECUTED against the pushed 300 at explicit SHA `f2fcec4`: **17 of 23 FAIL there**, exit 1 — not a skip. Battery **52 suites / 1,984 assertions, zero skips** (1,961 + 23, reconciled exactly); isolation guard 27/27; CRNA restamped to 301. `firestore.rules` untouched. |
| **163 (staff)** | _pending push_ | 22 Aug 2026 | **LOGIN-1 — THE FRONT DOOR NO LONGER CONTRADICTS THE BOARD.** Found LIVE, not by reading: the sign-in screen read **"PHASE 1 · BIDDING OPEN"** while the board behind it read *"Bidding is closed — no more changes can be made."* When the owner then Reset the auction entirely, the same badge flipped to "BIDDING CLOSED" — the mechanism, not a one-off. **Cause:** the badge asked `biddingOpen()`, which is only `currentPhase>1 || phase1Started===true` and consults NEITHER the admin's Close Bidding flag NOR timer expiry; the board has always used `isAuctionClosed()`, which checks both. **When it bit:** every interval between a phase's timer running out and the admin beginning the next one — four-plus times per auction, on the page every bidder lands on first. They read "bidding open", sign in, find it shut. **FIX, deliberately narrow:** `biddingOpen()` was NOT touched — it has **12 callers** on this page and is the wrong blast radius. `updateSigninPhase()` now asks `biddingOpen() && !isAuctionClosed()`, the same question the board asks. **Second half, without which the first is inert:** the badge was only ever recomputed by the PHASES listener, so a closure happening while someone sat on the login screen never reached them — the TIMER listener now calls `updateSigninPhase()` too. **GATES:** new `tests/test-staff-163-signin-badge.mjs` — 10 assertions executing the real extracted `updateSigninPhase` over `biddingOpen`/`isAuctionClosed` across the state matrix (open · flag set · timer expired · later phase · never started · no phase). Honesty EXECUTED against the pushed 162 at the explicit SHA `719566c`: **4 of 10 FAIL there**, exit 1 — not a skip. Battery **51 suites / 1,961 assertions, zero skips** (1,951 + 10, reconciled exactly); isolation guard 27/27; CRNA restamped to 163. |
| **300 (admin)** | _pending push_ | 22 Aug 2026 | **RA-4 H-1 + M-1 — THE TWO THE OWNER RULED NECESSARY (DECISIONS §87: *"i only want to do 100% necessary fixes at this point"*, then *"ignore other items"*). Nothing adjacent was touched (§85).** **① H-1 — five consumers of the frozen projection that build 298 did not repoint.** §77 froze the projection and 298 moved three "how full is this week" callers onto `weekLedger`; five more still subtracted the projection's `fteWon` from the cap. That is not the same number: swept over 5,000 decided week states, the two disagree in **82.1%** of them, worst gap **3 FTE**, and in BOTH directions — 3,147 report a week FULLER than it is (the owner's own §82(d) case: a capacity-3 week whose bidders were all denied reads 0.2 remaining instead of 3) and 960 report it EMPTIER. Repointed, one line each: the ✓ Approve as Win dialog on Draws & Reviews (which printed *"This approval fits within capacity"* at the moment the other Approve button warned — the two now agree, and the total shown is what is really spoken for); the **Cap · Winning · Remaining** header on BOTH decision panels (**Remaining** only — **Winning** is a projection readout and correctly stays one, §77/§82(b)); `weeksNeedingLock` and `lockedWeeksWithAvailability`, which WRITE locks and had drifted away from `toggleWeekLock`, already repointed in 298 — so two lock surfaces disagreed about the same week; `_reopenRoundMonths`, which could tell the admin a round has nothing to open when the weeks are in fact empty (its NE-2/NE-3 guard again means what its comment claims); and the rehearsal simulator's fit test, whose recompute-after-every-write had become a no-op. **② M-1 — one row per person per week on Draws & Reviews.** Because the projection is frozen, an approved person stayed in the draw set and was drawn a SECOND time as a bare row wearing no chip and carrying a live ✕ Deny, whose handler removes the approval — one click on a row showing no sign of a decision destroyed it. A decided person is now rendered ONCE, wearing that decision, with the non-destructive Revoke in place of Deny; the randomizer wheel reads the draw rows, so decided people leave it too. Deny's own confirm now names an approval it is about to remove. **GATES:** new `tests/test-admin-300-capacity.mjs` — 35 assertions, every behavioural one EXECUTING extracted code (both engines run over 5,000 week states per §82(d)'s standard; the lock helpers, both Approve dialogs, the round opener and the real Draws & Reviews render all driven in sandboxes). Honesty check EXECUTED against the pushed 299 built from the explicit SHA `903ab97`: **17 of 35 FAIL there**, exit 1 — not a skip. Full battery **50 suites / 1,951 assertions, zero skips** (1,916 + this suite's 35, reconciled exactly); schedule isolation guard 27/27; CRNA restamped to 300. `firestore.rules` untouched. |
| **162 (staff)** | _pending push_ | 21 Aug 2026 (evening) | **RA-4 H-3 + H-2 — A BID THAT DOES NOT SAVE MUST SAY SO.** Both findings are one sentence from a doctor's side: *I pressed the button and nothing happened.* **① H-3 — the refusal that was never on screen for a single frame.** Build 160 routed `showError` into the open dialog's `.dlg-err` slot, beside the heading — right for a message about that dialog's own dropdown. But three gate refusals (`bidding hasn't started`, `bidding is closed`, `this week is locked`) and the failed-save path all do `showError(...); closeModal();` with **no await between them**, and `closeModal` strips `.show`, whose CSS is `display:none`. RA-4 measured this in a DOM harness over the real functions: the message is visible for **zero frames**, every time, then `_clearDlgErrs()` wipes it at the next open. Under enforced App Check a bot-scored device gets `permission-denied` on every write, permanently — this is the path it lands on, and it said nothing at all. FIX: `closeModal` now RESCUES a still-showing slot message into the floating banner on its way out (the banner is `position:fixed` since 159, so it survives), and `confirmPriority` clears the slot on the SUCCESS side so a corrected-then-accepted bid never drags a stale alarm out with it. One function, so all four sites and any future one are covered — chosen over swapping four call sites, which a fifth site could escape. **② H-2 — the write that never answers.** RA-4 ran the real Firebase 10.12 client offline: `writeBatch().commit()` **neither resolves nor rejects — it pends for ever**, while the latency-compensated listener echo fires and `startListening` does `data=newData; scheduleRender()` with no metadata check, painting the bid as saved. The confirm dialog then hangs open (its trailing `closeModal` is never reached), the 8s safety timer frees the save lock in silence, and the write — memory cache only, no `persistentLocalCache` — dies with the tab. FIX: new `_awaitWrite(p)` races every write the confirm dialog awaits against `WRITE_DEADLINE_MS=10000` (both bid commits, the not-found fallback, the priority floor, the bid timestamp, the lowering counter); the save-lock safety timer goes 8s → 15s so the lock can never be freed mid-write. The message is deliberately **not** "failed" — the write may still land when the connection returns — it reads *"Couldn't confirm your bid — your connection may have dropped. Check the board before you leave this page."* A genuine refusal keeps its own identity and still reports as a failure with its reason (asserted). Gates: NEW `test-staff-162.mjs` **38/38**, EXECUTING the real extracted `_awaitWrite` against real timers (never-settling → rejects with a distinguishable code · a landing write passes through · a `permission-denied` is re-thrown unchanged, not laundered · a slow write is reported AT the deadline) and the real extracted `closeModal` + `flash` + `_openDlgErrSlot` against a hand-rolled DOM stub (no new dependency); honesty `--pre` vs the pushed 161 from the explicit SHA `2cd6a55` **FAILED 21 of 32, exit 1**, and the 11 that pass are exactly the must-keep-working ones (the refusal still lands in the dialog, a clean close raises no banner, closeModal still resets to step 1). **The battery caught a real regression and it was fixed the §82(f) way, never with a stub:** four suites sandbox `persistScheduleChange`/`saveBestBid` and went red on `_awaitWrite is not defined` — `test-audit-fixes`, `test-c2-priorphase-rebid`, `test-staff-158`, `test-staff-highs-155` now each build the sandbox with the REAL extracted `_awaitWrite` and the REAL `WRITE_DEADLINE_MS`, falling back to a pass-through ONLY when the source under test predates them — which is exactly what those builds did, so their own honesty runs stay faithful. Auction battery **49 suites / 1,916 assertions** green, zero failures · schedule **6 green, 21 Playwright-skipped on the device (no chromium there)**, `isolation-test` among the green — an auction-staff-only change touches nothing the schedule reads · CRNA restamped 162/299 · `index.html` and `versions.json` md5-verified device==cloud · all five inline script blocks `node --check` clean. Admin page untouched. |
| **161 (staff) · 297 (admin) · rules** | _pending push_ | 21 Aug 2026 | **THE RA-3 CLEAN-UP WAVE — seven findings the owner judged capable of real trouble, plus the sweep-harness guard.** **STAFF 161:** ① `getCurrentUser()` read the roster dropdown, and a `<select>` auto-selects its first `<option>` — so from roster-load until sign-in completed, every render painted the ALPHABETICALLY FIRST person's bid pool and a lowerings counter showing the full allowance. A doctor signing in could see another doctor's used numbers, and a count that was not theirs, and act on it. It now returns the signed-in identity or nobody; `render()` already bails on nobody, so the board simply stays unpainted (try/catch because the function is defined above the `let`, so an early call would hit the temporal dead zone). ② A sanctioned lowering writes twice — floor DOWN, then counter up. A refused second write left the bidder holding the benefit uncounted with nothing to re-derive it, under a message promising it would "self-correct… no action needed". The spend is now its own attempt in both branches and says plainly that it will NOT correct itself, and to tell the admin. **ADMIN 297:** ③ Begin Phase counted its bulk delete as "N denied bids retired" — it removes every leftover bid from a finished phase whose owner did not win, the denied ones AND the far more numerous ones that simply lost; toast and dialog now say so. ④ The Draws & Reviews destructive dialog put "Cancel Bid" beside the shared modal's grey "Cancel" — one word, two opposite meanings, side by side; it is "Remove Bid" now, matching its own body text. ⑤ "Disable Timer" described only what vanishes from the doctors' screen; it also removes the deadline that ENDS the phase, and now says so. ⑥ `saveTimerRules` swallowed its write error and announced success regardless — on the card that decides when bidding closes; it now fails honestly and returns false. ⑦ The NE-14 "Bidding is still OPEN" strip read a switched-OFF countdown as "bidding has finished" — the exact opposite, since nothing expires with the timer off; only a timer that is ON and RUN OUT now counts as evidence. ⑧ "Results were already sent to all N users" counted only doctors WITH an address; anyone without one was skipped and never told, on the very click where the admin concludes the job is done — both the phase and round toasts now name them and warn. ⑨ Delete All Users destroys strictly more than Reset Auction beside it (roster, login-e-mail map and display names on top of everything Reset preserves) yet carried neither of Reset's gates; it now takes the same feed-health check and cloud-backup offer, in the same order, before its dialog. **RULES:** ⑩ the quota-meter clause demanded `request.method == 'update'`, but the staff pages bump it with a merge-write that Firestore classifies as a CREATE while the document does not exist — so on the fresh CRNA project every staff-side send was refused and swallowed, and the 2,000-message ceiling could arrive with the gauge still looking healthy. A create branch was added under the same one-key containment. **⚠️ NOT VERIFIED BY CLAUDE — the emulator cannot run in the work environments. `tests/RA-2.command` on the owner's Mac is the proof, and the console publish must precede the push.** Gates: NEW `test-staff-161.mjs` **13/13** (executes the real `getCurrentUser` under three scopes incl. a simulated temporal-dead-zone read) honesty vs pushed 160 at `ab6e4bd` **3/13, exit 1** · NEW `test-admin-297.mjs` **29/29** (executes the real `saveTimerRules` against a stubbed Firestore, both outcomes) honesty vs pushed 296 at `ab6e4bd` **6/29, exit 1** · two legitimate pin updates, each annotated in-file (`test-admin-295-db1` retire wording; `test-audit-fixes` bookkeeping-message count 2→4, now split so the self-correcting and non-self-correcting cases are asserted separately) · auction battery **45 suites / 1,817** green · schedule **27/27** green · CRNA restamped 161/297 · every file md5-verified both sides. |
| **gates** | _pending push_ | 21 Aug 2026 | **TWO GATES MADE HONEST, found during the session's own verification pass.** ① `audit-handlers.mjs` is documented as "expect 0 violations" and had started printing **1**: admin/index.html carries a COMMENT explaining the `_jsq` escaper which quotes `onclick="fn('${_jsq(...)}')"` as an example, and the scanner has no notion of comments, so it read the example as a real handler calling a function called `fn`. A gate whose expected value is 0 and which always prints 1 either sends the next session chasing a ghost or teaches everyone to ignore the number. Now **0 violations**, with the three comment-embedded examples (`renderOverview`, `_syncCaps`, `fn`) excluded and all 152 real onclick lines untouched — verified by listing them. NOTE ON METHOD: the first attempt tracked strings and comments together and got it backwards — an apostrophe inside an earlier comment ("don't") opened a phantom string and swallowed every `//` after it. The shipped rule is deliberately the dumbest one that cannot misfire: drop a line only when its first non-blank characters are `//`. ② Staff **161** gained a guard on both shared-roster e-mail writes (`saveEmail`, `declineEmail`): they took `getCurrentUser()` and wrote `vacations/emails[user]` with no check. In practice the KP prompt only opens after sign-in resolves, so this was never reachable — but `vacations/emails` is read by the Daily Schedule too, and a write keyed on an empty identity would put a junk key into a document neither site owns alone. It refuses instead. Gates: `test-staff-161.mjs` **17/17**, honesty vs pushed 160 **5/17 exit 1** · auction **46 suites / 1,847** green · schedule **27/27** · handlers **188/0** · isolation **27/27** · 4-pass sweep clean, exit 0. |
| **sweep harness** | _pending push_ | 21 Aug 2026 | **THE SWEEP CAN NO LONGER LOOK SUCCESSFUL WHEN IT TESTED NOTHING (RA-3 F-1, second half).** The first half — the missing App Check stand-in — shipped in `af57c09`. This is the reason it went unnoticed for two days: with every handler undefined the driver clicked 338 times on admin, logged 325 copies of ONE error, clicked ZERO controls on staff, then wrote a tidy report and **exited 0**. It now prints a verdict and exits non-zero when a site clicked zero controls, when errors outnumber clicks (the shape of a page whose script never ran, as distinct from individual broken buttons), or on any error at all; and a site that cannot be swept is REPORTED rather than thrown, so the run explains itself instead of dying on a stack trace. Proven both ways: with the App Check mapping removed the driver exits **1** naming the cause; restored, it exits **0** — `admin/cancel 317 · staff/cancel 13 · admin/confirm 254 · staff/confirm 13 clicks, 275 dialogs, 154 confirms, 0 errors`. Same principle START-HERE §6 already states for the honesty checks: a skipped check is a failed gate, not a pass. |
| **160 (staff)** | _pending push_ | 21 Aug 2026 (early) | **THE REFUSAL MOVES INTO THE DIALOG (owner, on 159: *"the please select a bid feature is funky how it is"*, then marked the spot — the title row, beside the heading).** 159 made every refusal visible by floating the banner at the top of the viewport; correct, but for a message about the dialog's OWN dropdown it floated far from where the reader was looking and read washed-out over the dark overlay. 160 gives `flash()` two destinations from ONE entry point: if a dialog is open and offers a `.dlg-err` slot the message lands there, beside that dialog's heading, in a solid red pill that does not shove the fields down; otherwise it falls back to the floating banner, still the only way to surface a refusal that has no dialog to live in (`This week is locked`, `Save failed`). Both steps of the bid dialog carry a slot, the confirm step gets its own so a step-2 refusal does not appear back on step one, and `_clearDlgErrs()` runs when the dialog opens so a stale refusal never greets the next attempt. All 17 `showError` call sites inherit the routing untouched. Gates: NEW `test-staff-160.mjs` **13/13**, executing the real extracted `_openDlgErrSlot` / `_clearDlgErrs` / `flash` against a DOM stub (dialog open → in-dialog and banner stays down · confirm step → the confirm step's slot · no dialog → banner fallback · clearing); honesty `--pre` vs the pushed 159 from the explicit SHA `13b59de` **FAILED 4 of 7, exit 1** · auction battery **44 suites / 1,799** green · schedule **27/27** green · CRNA restamped 160/296 · md5-verified both sides. Admin page untouched. |
| **159 (staff)** | _pending push_ | 20 Aug 2026 (night) | **THE CLOSED-PHASE EXPERIENCE, AND THE REFUSALS NOBODY COULD SEE (owner ruling §78 + RA-3 finding F-2).** ① **The refusal banner now FLOATS.** `#errorMessage` sat in the page flow near the top while every bid is made far down the board behind a centred dialog — so all 17 `showError` messages, **"Save failed" among them**, painted ~800px above the viewport and were never seen. Reproduced live on 158: pressing Continue with no bid chosen ran `showError("Please select a bid")` and the page did nothing at all. Now `position:fixed`, centred, `z-index:100000` (above the dialogs), with a `flashIn` keyframe that preserves the horizontal centring — still self-dismissing, nothing to click. ② **The "Auction Closed" pop-up is REMOVED** (owner: *"it actually just gets in the way"*). It fired at the close of EVERY phase and told doctors the auction had ended — three phases early — and it interrupted to announce a state the board was already showing, since closing a phase calls `_lockEveryWeek()` and a locked card paints greyed, red-tinted, `cursor:not-allowed`, badged 🔒 LOCKED. Replaced by `#phaseClosedBanner`, a stated persistent line that names the PHASE, refuses to appear over the login screen, says the true general sentence when no phase is known (never "Phase 0"), and clears if the admin restarts the timer. ③ **The three gate refusals stop interrupting** — not-started, bidding-closed and week-locked move from `showCenterAlert` to the floating banner (13 sites), and every user-visible "the auction has closed" / "the auction window has closed" string is gone. Gates: NEW `test-staff-159.mjs` **21/21**, executing the real extracted `_setPhaseClosedBanner` against a DOM stub (raise, clear, pre-sign-in refusal, unknown-phase wording) plus CSS/markup pins; honesty `--pre` vs the pushed 158 from the explicit SHA `d49cd15` **FAILED 12 of 16, exit 1** · auction battery **43 suites / 1,786** green · schedule battery **27/27** green · CRNA restamped 159/296 · every changed file md5-verified device==cloud. Admin page untouched. |
| **rules only — no build bump** | _pending push_ | 20 Aug 2026 (night) | **RA-3 — the two world-readable auction documents are closed (owner ruling §76: fix data exposure only when the fix is cheap; this one is).** `changesArchive` joins `isAdminReadDoc`, `welcomeLog` joins `isSensitiveDoc`. TWO functional lines; every other line in the diff is comment. `changesArchive` held the merged bid + DECISION trail written by completePhase — which runs BEFORE results are e-mailed — and was on no read gate at all, so the approve/deny outcome of every completed phase, plus every bid ever placed, was readable with no sign-in; it was already admin-WRITE-only, so only the read had been missed. `welcomeLog`'s keys are participants' e-mail addresses via `_wlKey` (lower-case, non-alphanumerics → underscore — trivially reversible), and the file's own comment calling it "no PII" was wrong; that comment is corrected in the same edit. NO PAGE CHANGED: the staff sites never reference `changesArchive`, and read `welcomeLog` only from the sensitive-listener set and inside `welcomeOnce()`, both post-sign-in; the admin sites subscribe to `changesArchive` in the same block as approvals/denials/phaseStaging, already gated identically. Gates: `rules-emu/assertions.mjs` +11 assertions (6 of them new-gate), `PRE_RULES` override added so the SAME assertions run against the pre-RA-3 rules from the explicit SHA `d49cd15`, where the six MUST fail; `RA-2.command` gained step 4 so both runs are one double-click. **Claude could not execute either run — no emulator jar in this environment; the owner's Mac is the only place this suite is real.** Auction battery 42/1,765 and schedule 27/27 green on the unchanged pages. Console publish (BOTH projects) precedes the push. |
| **296 (admin)** | _pending push_ | 20 Aug 2026 (evening) | **§72 — FLOORS ARE ABSOLUTE + NE-14 (owner rulings).** ① Below-floor values (and NP in an NP-off phase) are HARD-refused on every admin bid surface — new adminFloorFatal/refuseAdminFloor (same contract as the 291 duplicate refusal), guardAdminBid + Add/replace entry checks + write-level backstops; the refusal dialog names the honest route ("change the floor/NP setting itself"). ② The build-266 approve override is RETIRED: _bfOverrideEligible returns false, the engine's approval-honoring no longer consults it, and adApprove refuses a floor-filtered click. ③ adminBidIssues #3/#3b retired (fatal gate owns them); CAPS, LOCKED WEEKS and Phase-1 scope stay warn-and-override (owner: retained). ④ NE-14 added to NEVER-EVENTS.md — verified the rules ALREADY refuse every user bid write once bidding closes (timerNotExpired + biddingNotClosed, 25 Jul audit); the Approvals & Denials panel now banners plainly when deciding while bidding is still OPEN. ⑤ ENGINE-RULES doc REVISED (32 rules, §70–§72 + NE-14) and re-delivered + committed. Gates: NEW `test-admin-296.mjs` 18/18 executing the real gate/backstop/engine/adApprove paths, honesty vs `1bdcb23` FAILED exit 1 · three legitimate pin updates (287 floor-warn pins, audit-fixes NP-off pin, capacity-float render-hook regex — spec changes, each annotated) · auction battery **42 suites / 1,765** green · schedule 6 green / 21 skipped · CRNA restamped 158/296 · md5 both sides. |
| **295 (admin)** | _pending push_ | 20 Aug 2026 (evening) | **DB-1 — "THE BOUNDARY FORGETS" (owner ruling §71).** Begin Phase N now retires every strictly-prior NON-WINNING live entry (schedule value + phase tag) in the SAME atomic batch that clears locks/decisions — the round rule (255) applied to phase boundaries; also sweeps legacy corpses from earlier phases. Winner check reads the MERGED archive view (published + the snapshot staged in that very batch). Never at denial time (revocability + the §70 block floor). Look-back kept whole per the owner's rule (bid + projection + result): round archives now store projections like phase archives (sandbox-guarded), and Edit Selections' completed-phase/round filters read the ARCHIVE schedule (read-only rows) instead of live leftovers; Begin-Phase dialog + begun toast state the retirement. Gates: NEW `test-admin-295-db1.mjs` 14/14 executing the real _commitBeginPhase and renderEditTable, honesty vs `1bdcb23` FAILED exit 1 (8 fails) · test-audit-fixes L2 window widened (a pin, not behavior) · auction battery **41 suites / 1,747** green · schedule battery 6 green / 21 skipped · CRNA restamped 158/295 · md5 both sides. |
| **157–158 (staff) · 292–294 (admin)** | _pending push_ | 20 Aug 2026 (day, owner away — standing go) | **THE RA-1 FIX WAVE (owner rulings §69–§70; order chosen by Claude, safest-first).** STAFF 157: RA-H6/L2 W-1 counter hidden when the feature is inert (allowance 0 / locks off) · RA-L1 plural follows the allowance · RA-H11 the remove dialog checks for a REAL weaker option before promising a lowering (floor+NP-aware, fails open) · RA-M2 welcome-e-mail timer sentence follows the reset mode · RA-M4 the revocation message forces the picker visible. STAFF 158: RA-H2 the 155 cancel fix vs the SDK's latency-compensated listener echo — pre-change tag captured at entry and re-injected for the synchronous change model (proven under BOTH orderings in the packaged audit harness) · RA-H5 the second auth handler re-checks auth.currentUser after its awaits. ADMIN 292: RA-H3/H4 C-4 stamp+guard+archived-round freeze on Cancel Bid AND Remove; announced-round-winner rows lock in Edit Selections ('🏆 Won — locked') · RA-H8 the bid dropdown _jsq-escapes both args · RA-M1 the manual-edit dialogs carry a phase-identity stamp into the raw writer · RA-H7 Add-Bid re-checks duplicates at WRITE time · RA-M6 confirmAdminEdit returns its promise into the M-14 net + mirror rollback (fn currently uninvoked at tip — noted in-code). ADMIN 293: RA-H1 the stranded-bids warning filters like the engine (locked winners / dead entries / announced rounds excluded, executed) · RA-M5 replace-dead-bid warns with the full isNew set (cap incl.) · RA-M3 Timer Rules header states the TR-4 batched save · RA-M2-twin mode-aware e-mail text. ADMIN 294 — **THE ENGINE (§70)**: denials are CAPACITY vs POLICY against the PRE-DENIAL natural projection (denied natural winner = policy, frees the spot, demotes nobody; denied CONTESTED-group member = capacity, strictly weaker bids stay blocked; denied natural loser = blocks nothing). Both audit repros fixed; Week-7/239 anchors all hold; twin-parity megafuzz 400 auctions × 35 users zero divergences; staff twin untouched (cannot see denials, NE-7). ALSO: firestore.rules PREPARED for RA-H13 (bestBids·changes·bidTimes·bidPhase·bidLowerings behind sign-in) + RA-M8 (mailStats update-only, one key, no removals) — delivered as captioned txt, **console paste = the owner's, NOT yet live; console validates on paste** · crna-stamp guard v3 (RA-L3, identifier-set pin, negative-tested: a count-preserving key swap ABORTS) · CRNA restamped 158/294 each build (also caught+remedied that 291's morning push had missed its restamp). RA-L4 DEFERRED (not expressible in rules; noted in TODO). Gates: 6 new executed suites (staff-157 16, staff-158 9, admin-292 20, admin-293 8, admin-294-engine 9 incl. a 2,000-scenario §70-oracle fuzz, sched ra-fixes 10), every honesty check EXECUTED against explicit-SHA fixtures 1bdcb23/dc6b8cc and FAILED non-zero · NE-1 oracles in test-never-events + test-priority-inversion moved to the §70 rule (mutation target updated) · p4-rounds pin now counts SIX refusal sites · auction battery **40 suites / 1,733** green ×4 · schedule battery 6 green / 21 Playwright-skipped (device) · isolation 27/27 · every file md5-verified both sides. |
| **291 (admin)** | _pending push_ | 20 Aug 2026 | **ES-1 + ES-2 (owner rulings, DECISIONS §69) — admin Edit Selections.** ES-1: a duplicate current-phase bid number is now REFUSED with an explanation on every admin bid surface (Edit Selections dropdown · Edit Bid dialog · Add/replace) — it was warn-and-allow, and the engine's I2 anti-forgery rule then scored BOTH weeks as no-bid. New `adminDupFatal` mirrors I2's competition filter exactly; write-level backstop in `_adminChangePriorityRaw`; reuse of a number consumed by a prior-phase WIN stays a warning (the engine honors that override); the engine is untouched. ES-2: an admin-set bid now BECOMES the week's priority lock (staff-lowering precedent build 139; simulator precedent) — both edit paths re-stamp instead of deleting, admin ADDs stamp too, NP stamps "NP" like the staff twin; dialogs and toasts tell the truth; deliberate unlocking stays on the Priority Locks page; remove/cancel still clear (the bid is gone). Gates: NEW `test-admin-291-edit-locks.mjs` 33/33 executing real extracted code · honesty vs pushed `f3d8e2a` (10/11 fail, exit 1) executed in BOTH environments · auction battery **35 suites / 1,671** green ×3 (one first-run flake immediately after the file transfer — dir-cache settling; never reproduced) · schedule code suites 5/5 green (21 Playwright suites skip on-device: no chromium there; unaffected by an auction-admin-only change) · 3 files md5-verified. |
| **156 (staff) · 290 (admin)** | _pending push_ | 20 Aug 2026 (overnight) | **W-1 + THE AUDIT'S LAST FINDINGS (owner orders, end of day).** STAFF 156: ① W-1 — muted “X of Y bid lowerings left” in the My Bid Count title bar (owner spec: plain text, the black-box spot, hidden when unlimited; Phase 4 shows the round’s numbers). ② MEDIUM — EXTERNAL session revocation (Google password change / sign-out-everywhere) now tears down the sensitive listeners and returns to sign-in with an honest message, instead of a board frozen on part-stale data. ③ LOW — _subscribeSensitiveOnce moved BELOW the registration check and its latch refuses anonymous sessions (no more dead mailQueue listener after an unregistered first attempt). ADMIN 290: ④ LOW — the two timer confirms derive the week count (53-Sunday 2028) · ④b **W-2** — the Phase-1 unlock warnings now agree in number ("1 standard week that IS…"), both call sites · ⑤ LOW — both lowering-usage archives (phase + P4 round boundary) now read the SERVER first (the 269 pattern), honest-toast fallback. Also: `crna-stamp.mjs` **guard v2** (audit LOW) — pins the variable-keyed storage-call count per page (staff 9 / admin 2); a future const-keyed storage key ABORTS the stamp until added to the rename table (negative-tested: a synthetic extra call aborted). CRNA restamped 156/290. Gates: NEW `test-staff-156.mjs` 14/14 + `test-admin-290.mjs` 9/9, both executing real code, honesty vs pushed `d619afe` (10/14 and 7/9 fail, exit 1) · full battery **34 suites / 1,638** green · mega-fuzz clean · inline blocks clean · 18 files md5-verified. NEW `test-rules-emulator.mjs` + `rules-emu/` — the FIRST executed firestore.rules suite (25 assertions: outsider/anon/stranger/registered/admin/dailysched); SKIPS LOUDLY here (emulator jar needs storage.googleapis.com, blocked in both work environments) — one-time enable: `cd tests && npx firebase setup:emulators:firestore` on an open network. |
| **housekeeping** | _pending push_ | 20 Aug 2026 (overnight) | **GUESSABLE STAGED BUILDS ARCHIVED (owner ruling: “archive guessable URLs”).** `build268-staged/` and `_to_delete/era268/` were SERVED by Pages with the live Firebase config (audit completeness-critic gap c). Moved to `_archive/vacation/` (grep-verified nothing references them); the push makes Pages stop serving them. Recorded in `_archive/README.md`. |
| **289 (admin)** | _pending push_ | 20 Aug 2026 | **AUDIT HIGH — ADMIN BID ACTIVITY RESETS THE COUNTDOWN (owner ruling, 20 Aug: the simulator precedent).** Admin manual bid add/edit/delete/replace-dead-bid and the Draws & Reviews Edit/Cancel all sent outbid alerts but never touched the timer, contradicting the staff Timer Rules text ('every time anyone places or changes a bid, the countdown resets'). New `_adminBidTimerReset()` — the simulator batch’s guarded reset verbatim — called at all FIVE outbid-alert sites. Guards preserved: M-6 (an EXPIRED timer is never reset — would silently reopen a closed phase), never inside the opening window, timer-off no-op, and 'on-but-never-started' STARTS the countdown, mirroring the staff page. CRNA restamped (155/289). Gates: NEW `tests/test-admin-timer-289.mjs` 7/7 (raw edit path EXECUTED end-to-end + the full guard matrix + all-five-sites pin) · honesty `--pre` vs pushed `ce427a2` FAILS 3/7, exit 1 · three sandboxes gained the reset stub · full battery 31 suites / 1,615 green · mega-fuzz 54k zero violations · inline blocks clean · all 7 changed files md5-verified device==cloud. |
| **288 (admin)** | _pending push_ | 20 Aug 2026 | **AUDIT HIGH BATCH H-D — the last two standing-go HIGHs.** (1) `sendReminderEmails` was the ONE mass-mail path with no entry gates: it now takes the same `_feedsHealthyOrExplain` check as every other sender (dead feeds read as an empty schedule — “no bids” for the whole roster) AND the shared `_resultsSendInFlight` guard held for the whole loop, released in finally — a second click mid-send is refused instead of re-mailing. (2) REPORT HONESTY: week headers labelled the committed+projected FTE figure “approved”, and the Weekly-Summary subtitle claimed approved-only figures — untrue mid-phase by finalFteForWeek’s own design comment. Header and Excel band now say “committed + projected”; the subtitle states the real composition. CRNA restamped (155/288). Gates: NEW `tests/test-admin-highs-288.mjs` 11/11 (sender EXECUTED with recorders incl. a hung-send double-click race; labels pinned where they render) · honesty `--pre` vs pushed `ce427a2` FAILS 9/11, exit 1 · full battery 30 suites / 1,608 green · mega-fuzz 54k zero violations · inline blocks clean · all 5 changed files md5-verified device==cloud. |
| **287 (admin)** | _pending push_ | 20 Aug 2026 | **AUDIT HIGH BATCH H-C — admin bid-entry guards.** (1) `adminBidIssues` gains the one bidder-facing rule it was missing: a BID FLOOR clause — a below-floor manual entry (incl. NP under any numeric floor) now warns that the engine will not score it and only an explicit Approve overrides. (2) The Draws & Reviews **Edit Bid** dialog skipped adminBidIssues entirely; its onConfirm now routes through the same `guardAdminBid` warning gate as every other entry surface. (3) APOSTROPHE SAFETY: per-user buttons interpolated raw roster names into single-quoted onclick JS strings with no escaping — a name like O’B (accepted by both sanctioned roster writers) made every button for that user a syntax error at click time. New `_jsq()` escaper (JS-escape, then entity-escape); ALL 52 single-quoted onclick arguments across the page now pass through it, and the Spin-Randomizer’s JSON user array is entity-escaped at both sites. CRNA restamped (155/287). Gates: NEW `tests/test-admin-highs-287.mjs` 11/11 (floor clause + Edit-Bid gate EXECUTED; escaping proven by an end-to-end attribute-decode + JS-parse round-trip of O’Brien) · honesty `--pre` vs pushed `ce427a2` FAILS 3/11, exit 1 · two suite sandboxes gained the new deps (audit-fixes ×2, lead-admin) · full battery 29 suites / 1,597 green · mega-fuzz 54k zero violations · inline blocks clean · all 7 changed files md5-verified device==cloud. |
| **286 (admin)** | _pending push_ | 20 Aug 2026 | **AUDIT HIGH BATCH H-B — the Fair Play pair (flag accounting only, nothing auction-critical writes here).** (1) `_fpWasWinning` treated a TIE as a winning position: two tied FTE-1 bids on a 1-slot week are a DRAW on every screen, yet a lowering/cancel from that state bumped winLower/winCancel — falsely branding an honest bidder. Fixed: ties count toward used slots (`<=`), matching getOutcome and the function’s own spec comment; a multi-slot tie where everyone fits still reads as winning. (2) The overLower cross-check counted every ‘lowered’ event in the live PHASE-4 change log against a single ROUND’s allowance (round boundaries never reset changesRef) — one legal lowering per round read as “client gate bypassed”. Fixed: phase 4 fences on the latest p4RoundResultsSent stamp (bidding is locked between results-sent and round start; round 1 keeps fence 0); phases 1–3 unchanged. CRNA restamped (155/286). Gates: NEW `tests/test-fp-286.mjs` 8/8 executing the real Fair Play block · honesty `--pre` vs pushed `ce427a2` FAILS 2/8, exit 1 · `test-fairplay.mjs` line-106 pin CORRECTED (it pinned the buggy tie behavior) + a fits-tie companion added · full battery 28 suites / 1,586 green · mega-fuzz 54k zero violations · inline blocks clean · all 6 changed files md5-verified device==cloud. |
| **155 (staff)** | _pending push_ | 20 Aug 2026 | **AUDIT HIGH BATCH H-A — four verified staff-page HIGHs in one build (owner: "combine a few").** H1 · the CANCEL-path sibling of C-2: the local phase tag was deleted BEFORE the change model ran, so getUserBidPhase fell back to a completed-phase archive and judged the live bid dead — a cancel flipping a competitor LOSE→WIN sent no mail and (modes 2/3) no reset; the tag delete is now DEFERRED until after the timer/e-mail block. H2 · an App-Check-blocked bidder (permission-denied — permanent for that device) was told "temporary connection problem — try again"; the error is now captured and permission-denied gets an honest message pointing at the admin (FB-4). H3 · the cancel dialog asserted the absolute priority-lock floor in EVERY branch; it now states the rule actually in force — unlimited / "X of Y bid lowerings left" (the owner’s W-1 wording — the dialog half of W-1 ships here) / exhausted / lock-off. H4 · the bid pool showed NP as a green remaining chip in phases where NP is not allowed; it now follows npAllowedNow() (muted chip + reason otherwise). CRNA restamped (155/285). Gates: NEW `tests/test-staff-highs-155.mjs` 16/16 (H1/H3/H4 EXECUTED against real extracted code — H3 read as a person per the human-lens rule; H2 pinned structurally, inline auth callback) · honesty `--pre` vs pushed `ce427a2` FAILS 10/16, exit 1 · full battery 27 suites / 1,577 green · mega-fuzz 54k zero violations · 4 inline blocks node --check clean · all 5 changed files md5-verified device==cloud. |
| **285 (admin)** | _pending push_ | 20 Aug 2026 | **AUDIT C-4 + BEGIN-PHASE SIBLING — ACTION-TIME PHASE-IDENTITY GUARDS on every decision writer.** The [build 245 H2] rule completePhase carries now covers adApprove/adDeny/adRevokeApprove/adRevokeDeny/confirmApprove/confirmRevokeApproval and BOTH bulk loops: `_decisionStamp()` at dialog-open, `_decisionGuard()` first line of every onConfirm — refuses when the phase (or Phase-4 round) moved, and for approve/deny also when the bid is no longer current-phase; bulk approve re-filters its precomputed list per item. `_commitBeginPhase` gains the same identity check (stale confirm cannot wipe the live phase’s decisions, regress currentPhase, or re-arm the timer; Begin-Phase-1 refuses once phase 1 started). Battery caught TWO things and both were fixed: the refusal toasts initially claimed “nothing was changed,” which build 234-L2 BANNED in Begin Phase (locks may already have moved) — wording corrected; and four suites slice _commitBeginPhase by fixed length — widened per their own convention. CRNA restamped (154/285). Gates: NEW `tests/test-c4-phase-identity.mjs` 20/20 EXECUTING the real writers through open→advance→click (phase, round, bid-level, bulk, Begin-Phase ×4) · honesty `--pre` vs pushed `6448f17` FAILS 12/20, exit 1 · five suites updated (4 slice widenings + H2-deny sandbox gains the guard stubs) · full battery 26 suites / 1,561 green · mega-fuzz 54k checks zero violations · 4 admin inline blocks `node --check` clean · all 10 changed files md5-verified device==cloud. Ships in ONE push with 153+154. |
| **154 (staff)** | _pending push_ | 20 Aug 2026 | **AUDIT C-2 — PRIOR-PHASE LEFTOVER RE-BID JUDGED “NO CHANGE” (double-confirmed CRITICAL).** Lost/denied bids linger in the schedule doc across phase boundaries, and `persistScheduleChange` built its change model from the RAW leftover — after the optimistic tag write had already reclassified it as current-phase. A Phase 2+ re-bid on such a week: same value → no timer reset in ANY mode and zero outbid mails; different value → not a new bid (mode 3 skipped) and a before/after pair hiding the displacement. Fix: capture `isCurrentPhaseBid` at ENTRY; a non-current leftover is modeled as NO previous bid (`_prevVal` masked → new-bid semantics) and removed from a `_beforeSnap` copy that the timer gate AND `sendEmailNotifications` now both judge — same phase-visibility rule as the cap check in the same function. CRNA restamped (154/284). Gates: NEW `tests/test-c2-priorphase-rebid.mjs` 27/27 EXECUTING the real persist path over stubbed Firestore (8 scenarios: both leftover paths + 6 regressions incl. extend-only H5/H8 and the opening window) · honesty `--pre` vs pushed `6448f17` FAILS 7/27, exit 1 · two pinning suites updated to the fixed model (`test-c7-timer-bundle` structural pins → `_beforeSnap`; `test-audit-fixes` sandbox gains `isCurrentPhaseBid`) and the full battery re-run green — 25 suites / 1,541 · mega-fuzz 54k checks zero violations · 4 inline blocks `node --check` clean · all 7 changed files md5-verified device==cloud. Ships in ONE push with 153. |
| **153 (staff)** | _pending push_ | 20 Aug 2026 | **AUDIT C-1/C-3 — THE SUMMER-FLOOR TWIN SPLIT (double-confirmed CRITICAL, found independently by two blind lenses).** Staff `weekMeta` derived `summerOnly` from the week’s DECORATION: any SPECIAL_WEEKS label short-circuited the summer fallback, and non-HD holiday entries never carry the flag — so the Memorial Day and July 4th weeks (auto-appended non-HD by the calendar editor, inside the summer window) lost the summer bid floor on the STAFF side only, while the admin engine classifies by date range. A below-floor or NP bid was accepted, shown WINNING, then silently dropped at award (F-6 twin violation, lost bid). Fix: ONE line — `summerOnly:isSummer&&!special?.highPriorityOnly` (date-window derived; HD precedence kept; decoration untouched). CRNA restamped (153/284). Gates: NEW `tests/test-summer-floor-twin.mjs` 19/19 executing the real builders + floor functions from BOTH pages · honesty `--pre` vs pushed `6448f17` FAILS 7/19 (staff=none admin=8 on both weeks) · full auction battery 24 suites / 1,514 green incl. crna-stamp · mega-fuzz 54k checks zero violations on fixed bytes · 4 inline blocks `node --check` clean · all 5 changed files md5-verified device==cloud. |
| **152 (staff)** | `29cffa0` · pushed 19 Aug | 20 Aug 2026 | FB-5 STAGE 2 BATCH 2 — `locks` · `fteMap` · `bidPhase` · `slots` moved from module load (anonymous bootstrap) into `_subscribeSensitiveOnce()`, beside batch 1; teardown resets all four caches and re-zeroes weekMeta capacity via `applySlotOverrides()`. Verified reader-by-reader that nothing pre-sign-in consumes them (render() and every bid-flow guard sit behind sign-in; `signinReady()` waits only on adminSettings). PLAN CORRECTIONS against the code: **mailStats had NO staff listener** (write-only `trackEmailSent`; the admin listener already sits behind authReady) — the batch is FOUR, not six; **timer deliberately NOT moved** (its callback runs `startCountdownTick()`) — it ships last, alone. Nothing is GATED by this build. CRNA restamped. Gates: `test-appcheck-login` grows to 84/84 — the new block asserts the four attach ONLY inside `_subscribeSensitiveOnce` on BOTH staff pages, the teardown resets, and timer stays pinned at module level; honesty from EXPLICIT pushed SHA `b7a0c3c` fails on both pages (git-read, no /tmp fixture) · FULL auction battery 23 suites / 1,495 green ON-DEVICE · schedule isolation + the 3 other non-Playwright suites green on-device · ⚠ **the 21 Playwright schedule suites DID NOT RUN**: no chromium on the device, and in-cloud they fail for a PRE-EXISTING reason — the pages import `firebase-app-check.js` since 281/146 but the suites' gstatic swap list never gained it, so in a network-restricted browser every page-load suite dies at boot. Schedule bytes are UNCHANGED from their last green run; gap filed as TODO **PW-1** and must be fixed before the audit baseline. `node --check` clean ×4. |
| **staff 151** (admin unchanged) | `60df92f` · pushed 19 Aug | 19 Aug 2026 | **FB-5 STAGE 2, BATCH 1 — six board listeners moved behind sign-in.** `changes` · `bestBids` · `bidLowerings` · `bidTimes` · `whitelistConfirm` · `welcomeLog` attached at MODULE LOAD, during the ANONYMOUS bootstrap — which is precisely why those documents have to stay world-readable. Each feeds ONLY the bidding board, which is hidden until sign-in; **every use of every one of the six variables was read before moving it**, not sampled. They now attach inside `_subscribeSensitiveOnce()` beside the PII documents that already lived there, and are torn down with them on sign-out — the teardown also drops their caches and resets `_whitelistLoaded`, because a rejected snapshot listener NEVER retries on this page and stale data must not survive into the next session. **NOTHING IS GATED BY THIS BUILD and there is no user-visible change** — it removes the obstacle so the rules can be tightened one document at a time afterwards, each proven separately (FB-5 stage 2 method). Admin page untouched: its listeners already defer behind `authReady`. CRNA re-stamped. **THE BATTERY CAUGHT A REAL WEAKNESS:** `test-delta-fixes` §10 asserted the teardown list held exactly THREE listeners. Beyond being wrong after this change, that count form tested the wrong property — a bare `onSnapshot()` added inside the function WITHOUT the `_sensitiveUnsubs.push()` wrapper would keep the count at 3 and PASS, leaking exactly the zombie listener that section exists to prevent. RE-ANCHORED to the real invariant (attaches === tracked), which is STRICTER than what it replaced. Gates: `test-appcheck-login.mjs` 72→78, honesty baseline `893879a` (the build that still attached them at module load) · `test-delta-fixes` 92/92 · FULL auction battery 23 suites / 1,489 green · schedule battery green · isolation 27/27 · crna-stamp 30/30 · node --check clean. **✅ VERIFIED LIVE by the owner, 19 Aug: signed in on the real site and the board populates normally.** For a change that only alters WHEN data loads, "it looks exactly the same" IS the pass condition, and it was met. |
| **staff 150** (admin unchanged) | `893879a` · pushed 19 Aug | 19 Aug 2026 | **FB-5 PREP — the retired passcodes listener is deleted from the staff page.** Passcodes are retired (START-HERE §3 rule 5), but this page still held a LIVE listener on `vacations/passcodes`, wrote it into `passcodesData`, and never read that variable anywhere — confirmed by grep across the whole file before cutting. Worse, `signinReady()` BLOCKED the Continue button on a document belonging to a dead feature. **Three wins, one deletion:** one fewer document read on EVERY page load for EVERY client (FB-1 arithmetic), a marginally faster sign-in, and — the reason it was done now — **`vacations/passcodes` drops off the "must stay world-readable" list in FB-5**, because no pre-sign-in code reads it any more. **Deliberately NOT touched: the ADMIN page still owns this document** (clear one password · clear all · Reset Auction) and `adminSettings.passcodesEnabled` stays a stored field — the suite now asserts the admin path SURVIVES, so a future cleanup cannot delete it by pattern-matching the same name. Admin page bytes unchanged; CRNA re-stamped. Gates: `test-appcheck-login.mjs` 64→72 with its own honesty baseline `85f61e7`/`0d84ece` (the builds that still had the listener) · FULL auction battery 23 suites / 1,482 green · schedule battery green · isolation 27/27 · crna-stamp 30/30 · 22 inline blocks node --check clean. |
| **rules only** (no build) | `85f61e7` · pushed + **PUBLISHED 19 Aug** | 19 Aug 2026 | **FB-5 STAGE 1 — `vacations/schedule` is no longer world-readable.** That document holds EVERY participant's complete bid list for every week, and the read rule's public default meant anyone holding the (public by design) Firebase config could read the entire live auction WITHOUT SIGNING IN AT ALL. New `isBidDataDoc(docId)` predicate; the read gate becomes `(isSensitiveDoc || isBidDataDoc) ? isVerifiedAccount() : true`. **NO CLIENT CHANGE, no build number, no page bytes** — which is exactly why this was chosen as stage 1. Proven safe on all four surfaces BEFORE editing, not assumed: staff attaches its bids listener only inside `startListening()` on the post-sign-in path · **admin pages defer every listener behind `authReady`, which is resolved only inside `_enterAdmin()` — i.e. after a verified admin sign-in** (checked precisely, because an admin locked out of their own bid data would be the worst possible outcome) · `mobile.html` carries no Firebase · the Daily Schedule site never references the document (grep + the isolation test's own ref discovery). **`isVerifiedAccount` NOT `isRegisteredUser`, and that is a COST decision as much as a security one:** `isRegisteredUser` costs an `exists()` + `get()` on loginEmails, and rules document-accesses are BILLED AS READS — on the most frequently delivered document in the auction that is a recurring per-delivery charge on every bidder's session (see FB-1). `isVerifiedAccount` reads only the auth token: zero lookups, zero cost, and it is the same bar the sensitive PII documents already use. It closes the anonymous-stranger hole; the any-Google-account case is App Check's job (FB-4), not this rule's. Tightening later is a one-word change. **The battery caught a real thing:** `test-audit-fixes` asserted the ordering of the read gate by matching the literal `isSensitiveDoc(docId) ? isVerifiedAccount()`, which this change reshaped — indexOf returned -1 and the check failed on a change that reordered nothing. RE-ANCHORED to a regex, **not weakened**, and four new assertions added pinning that the bids document is gated and precedes the public fallthrough. Gates: auction battery 23 suites / 1,474 green · schedule battery green · isolation 27/27. **PUBLISHED AND VERIFIED LIVE 19 Aug 2026.** The owner published, then Claude PROVED which version was live rather than taking it on trust — from an ANONYMOUS session on the real site, four probes: `vacations/schedule` **DENIED (permission-denied)** — the gate is live; `vacations/emails` DENIED (control: already gated, unchanged); `vacations/userList` ALLOWED and `vacations/phases` ALLOWED (controls: these MUST stay public or the sign-in screen cannot render — see the FB-5 stage 2 map). So the hole is closed and the login bootstrap is intact. Signed-in reads are covered by the same `isVerifiedAccount` predicate that has gated the PII documents in production for weeks. |
| **284 · staff 149** | `613bb56` · pushed 19 Aug | 19 Aug 2026 | **FB-6 — the security box now shows on BOTH sign-in panels.** Every sign-in screen has two sibling panels and shows exactly one: a WELCOME BACK panel for anyone with a remembered name, and a picker panel for everyone else. **"Remember me on this device" is checked BY DEFAULT**, so after a first sign-in effectively all 35 bidders get the welcome panel — and the box lived inside the PICKER, so they saw neither the owner's security line nor, since 283/148 hid the badge, **the reCAPTCHA attribution Google REQUIRES to be visible in the user flow.** A UX gap that became a terms gap. Found by loading the live site, not by reading code — the giveaway was Claude's own browser being remembered. Fix: the box moves OUT of the picker to sit after both panels inside their shared parent, ONE copy, rendering under whichever panel is showing; a comment at the site says not to duplicate it into the panels, which would only drift. **Applies to all four hand-edited pages — verified, not assumed:** the admin pages use the same two-panel pattern under different ids (`authGateCard`/`authGateWelcome` vs `signinPicker`/`signinWelcome` vs `signPicker`/`signWelcome`), so a fix written for the staff ids alone would have silently missed half the site. Structural safety: the transform located each panel's matching close tag by depth-counting rather than by line number, and `<div>` open/close counts were diffed against HEAD for every file — delta (0,0) everywhere. (The admin pages read 863/861 in isolation; that imbalance is PRE-EXISTING markup built inside JS strings, confirmed identical at HEAD, not something this build introduced.) Gates: `test-appcheck-login.mjs` 52→64, the FB-6 block parsing each page's real panel spans and carrying its OWN honesty baseline `b162f3d`/`fb42ed7` (the builds with the box still inside the picker) · FULL auction battery 23 suites / 1,470 green · schedule battery green · isolation 27/27 · crna-stamp 30/30 · 22 inline blocks node --check clean. **Visual confirmation in both panel states is still owed and should be done on the live site after push.** |
| **283 · staff 148** | `b162f3d` · pushed 19 Aug | 19 Aug 2026 | **HIDE THE BADGE AGAIN, AND CARRY THE ATTRIBUTION (DECISIONS §66).** Owner reversed §65 the same day, having seen it live: *"I don't like the recaptcha being visible actually. it's in the way."* Nobody was wrong — §65 was built from what he asked for before it existed on screen. The un-hide CSS rule is REMOVED so Firebase's hidden default stands. **The consequence is not optional:** Google's terms allow showing the badge as served OR hiding it with the attribution visible in the user flow, and nothing between, so the sign-in security box gains a third small line in Google's own words with links to its Privacy Policy and Terms. **Now a PAIRED INVARIANT** — the suite asserts the un-hide rule is ABSENT *and* the attribution plus both links are PRESENT on all six pages, so a future edit that drops the attribution without restoring the badge fails the battery instead of quietly violating the terms; each page also carries a comment saying so at the point of temptation. **KNOWN GAP, accepted and tracked as FB-6 (now raised to next build):** the box sits inside the sign-in PICKER panel and "Remember me" is checked by default, so returning users see the WELCOME BACK panel and see neither the line nor the attribution. Pre-existing placement, but it now carries compliance weight too. NOT fixed here on purpose — it is structural DOM surgery across four differently-shaped files at the tail of a long session (START-HERE §4), so it was split rather than rushed. Gates: `test-appcheck-login.mjs` 52/52 with the badge block INVERTED and re-baselined to `e5fa0a2`/`4fa726d` (the builds that shipped it visible) · FULL auction battery 23 suites / 1,458 green · schedule battery green · isolation 27/27 · crna-stamp 30/30 · 22 inline blocks node --check clean. |
| **282 · staff 147** | `e5fa0a2` · pushed 19 Aug | 19 Aug 2026 | **SHOW THE reCAPTCHA BADGE.** Owner, on seeing 281/146 live: *"I don't see the recaptcha."* Diagnosed by inspecting the LIVE PAGE in a browser rather than re-reading the code: **Firebase App Check renders the badge inside a container it creates itself — `div#fire_app_check_<firebaseAppName>` — and puts an inline `display:none` on it.** The badge was present and working the whole time; Firebase hides it by default. Claude had told the owner it would be visible, which was wrong, and it was the owner using his own site that caught it. Fix is ONE rule: `div[id^="fire_app_check"]{display:block!important}`. **PREFIX selector on purpose** — the id embeds the Firebase app name, which differs on every page (`user-site` · the default app · `sched-user-site` · `sched-admin`), so a hardcoded id would cover one page and silently miss three; the suite fails a hardcoded form. The badge itself is NOT restyled or repositioned: Google's terms allow showing it as served or hiding it with attribution and nothing between, and its `right:-186px` collapsed tab is Google's design, not a defect. Showing it also removes the attribution obligation entirely — before this build the site sat in NEITHER permitted position (DECISIONS §65). Verified in a real browser BEFORE shipping: with the rule applied the badge measures 294×69 at bottom-right with the standard logo tab on screen. Also confirmed live on 281/146 while diagnosing: zero console errors, and the new 3000 ms sign-in readiness fallback did NOT fire — the FB-4 load-timing risk, checked against the real deployment instead of assumed. Gates: `test-appcheck-login.mjs` 40→52, the badge block carrying its OWN honesty baseline (`5aa89fa`/`324c797`, the App-Check builds that shipped with the badge still hidden) because reusing the pre-App-Check baseline would have made one generation of checks vacuous · FULL auction battery 23 suites / 1,458 green · schedule battery green · isolation 27/27 · crna-stamp 30/30 · 22 inline blocks `node --check` clean. Enforcement remains OFF and is now documented as a checklist in TODO FB-4 at the owner's request. |
| **281 · staff 146** | `5aa89fa` · pushed 19 Aug | 19 Aug 2026 | **APP CHECK + THE LOGIN SECURITY BOX.** Owner asked whether a stolen API key could run up a Blaze bill; the premise needed correcting (the browser key is public by design and always was), but the real exposure was found and is real: the `vacations` read rule ends in a public default, so anyone could read the roster and every live bid with no sign-in, and every read is metered. **Firebase App Check** now attests that a request came from THESE pages. Initialised BEFORE `getFirestore` on purpose — the first request must already carry a token, and this page's own comments warn that a listener rejected for a missing token NEVER retries, which is exactly how enforcement would lock everyone out. `isTokenAutoRefreshEnabled:true` (the SDK does NOT refresh by default; a stale token would begin failing silently mid-phase). **Owner ruling DECISIONS §63:** all six pages in ONE build, but **enforcement stays OFF** — it is switched on per service and per project from the console after monitoring shows clean traffic, and is a one-click revert. Nothing about sign-in changes for any user. **The 1500 ms sign-in readiness fallback → 3000 ms:** App Check puts a reCAPTCHA script load AND a token fetch in front of the first Firestore request, and the old guard — chosen when Firestore answered in <500 ms — could fire before any data arrived and unlock the screen with an EMPTY person list. The warning now names which document was missing, so it is diagnosable from the console. **Login box (DECISIONS §64, owner ruling; Claude dissented and was overruled — reasoning preserved there):** the "Google will show a screen that says …" note is REPLACED by "🔒 Secured by Google sign-in and reCAPTCHA. / This site never sees or stores your password." Password claim VERIFIED true before shipping: the passcode path is retired and `updateLoginPwState()` is an empty stub. **`crna-stamp.mjs`:** the retired authDomain copy-swap is gone (no domain left in the copy to swap) and is replaced by an App Check site-key swap — CRNA is a separate Firebase project and an MD key would simply fail to attest there. `MD_FB.authDomain` and `MD_APPCHECK` join the FORBIDDEN canary, which is strictly stronger than what it replaced: the old code repaired a leak, the canary refuses to ship one. Gates: NEW `tests/test-appcheck-login.mjs` 40/40, EXECUTING the shipped init block against stubs so the key is proven to reach the provider rather than merely being present in the file · honesty built from EXPLICIT SHAs `50a5d97`/`34f19b7` read straight out of git (no /tmp fixture to be wiped) and measured as discriminating — 10/10 now vs 1/10 before on every page, not a skip · `test-crna-stamp` 27→30, its auth-domain assertion STRENGTHENED after it was found to have gone vacuous · FULL auction battery 23 suites / 1,443 green · schedule battery green · isolation 27/27 · `node --check` clean. |
| **279 + 280 · staff 145** | `50a5d97` · pushed 19 Aug | 19 Aug 2026 | ADMIN BID-FLOORS DIALOG — the last place still speaking the old language. Owner screenshot of the "Save bid floors?" confirm: it enumerated every allowed value (`bids 1, 2, 3, 4, 1/2, or 1/2/3`, and thirteen items at a floor of 10) while the bidders had just been switched to *"your bid must be 4 or better"* in 278 — so the admin could not check at a glance that the setting and what people are told agree. Owner's wording, verbatim: **"High demand weeks: 4 or better. Summer weeks: 10 or better. All other weeks: no floor — every bid allowed."** `bfAllowedText` is no longer called on the admin page (it stays defined, twin-paired, and still serves the STAFF bid-rejection alert where a bidder does need the actual choices — see the tidy-up note in TODO). **Stated trade, owner's call:** the dialog no longer mentions that ANY numeric floor blocks NP while "No floor" allows it; that consequence remains spelled out in the card description directly above the controls. Admin page only — staff bytes unchanged; CRNA re-stamped. **280 / staff 145, same push — owner ruling: "I don't want that type of wording anywhere."** The staff BID-REJECTION alert was the last place still enumerating: a bidder who submitted below the floor got *"This high-demand week only accepts bids of 1, 2, 3, 4, 1/2, or 1/2/3."* It now reads *"This high-demand week needs a bid of 4 or better."* — the same words as the rules list and the admin dialog. With that call gone, `bfAllowedText` had ZERO callers on either page, so it was DELETED outright rather than left as dead code that could regenerate the banned wording; `tests-build268.mjs` dropped it from the sandbox it no longer needs. A suite assertion now sweeps BOTH pages for any surviving run-of-numbers list of allowed bids. NOT touched, flagged for a ruling: *"A combined bid like 1/2/3 uses bids 1, 2, and 3 at once"* on the rules list and in the welcome e-mail — that defines what a combined bid IS rather than listing what is allowed, so it was left alone. Gates: `tests/test-copy-rules-wording.mjs` grows to 53/53, EXECUTING the dialog's own wording helper over every selectable floor (3–10 and none) and pinning that the dialog and the bidder sentence carry the same number in the same words · honesty `--pre` vs the pushed 278/144 fails 10 · FULL auction battery 22 suites / 1,403 green. NOTE: 279 was never committed before 280 was written on top of it, so no 279 fixture exists — the honesty baseline for both is the pushed 278/144, which is the stricter check anyway · schedule + isolation green · `node --check` clean. |
| **278 · staff 144** | `a5d0bb8` + follow-up · pushed 19 Aug | 19 Aug 2026 | RULES COPY — two owner-requested wording changes to generated text that feeds the rules list AND the welcome e-mail on BOTH sites. **COPY-1:** `prioLockBullets` collapsed only when all four phase allowances matched, so the live 2/4/4/2 spelled out four clauses and said "up to" four times. Adjacent phases sharing an allowance now merge and "up to" is said once → *"…up to 2 times in Phase 1, 4 times in Phases 2 and 3, and 2 times in each round of Phase 4."* Phase 4 NEVER merges with phases 1–3 (its count is per ROUND — merging would state something untrue), and any pattern containing a 0 or an unlimited keeps the old per-clause wording, because "not at all"/"freely" cannot sit under a shared "up to". **COPY-2 (owner's exact wording):** `bfRulesBullets` listed every allowed value, so a STRONGER floor produced a LONGER list — backwards to read. Now: *"On the highest demand weeks (…), your bid must be 5 or better · On summer weeks it must be 8 or better"*. Proven safe before writing: the engine's own `priorityScore` ranks the 1/2 and 1/2/3 combinations ABOVE every single number, so "or better" still includes them at every floor (pinned for floors 1/2/5/8). **COPY-3 (owner ruling, same session):** a floor that is NOT set now produces NO sentence — the rule is eliminated, never announced as absent — so with neither floor set the bullet disappears entirely and the old "currently have no bid floor — all bids may be used" sentence is retired. Both sentences also STAND ALONE now (no "it must be" leaning on the line above): the rules list joins them with a separator, but the welcome e-mail prints them as SEPARATE BULLETS, where a leading pronoun has nothing to refer to — caught by the owner. The staff rules list's hardcoded placeholder (which still read "only bids of 1, 2, 3, 4, 1/2, or 1/2/3" — old wording AND a floor that was never live) is now empty and hidden until the live floors fill it, and hides again when there is nothing to say. `bfAllowedText` is untouched — the explicit list is still the right form for the admin floors card and the bid-rejection alert. Both generators stay TWIN byte-identical; CRNA re-stamped. Gates: NEW `tests/test-copy-rules-wording.mjs` 41/41 executing both generators on both pages across all five floor configurations · honesty `--pre` vs the pushed 277/143 fails 19 · FULL auction battery 22 suites / 1,391 green · schedule + isolation green · `node --check` clean. |
| **277 · staff 143** | `23cd748` · pushed 19 Aug | 19 Aug 2026 | CHANGE LOG — system admin actions stop pretending to be bids. From an owner screenshot of three rows reading "Admin admin-timer-mode-1 bid for ALL on Wk 0 · undefined undefined NaN". **CL-1:** `adminLog` writes system-level actions (timer mode, restores, user removal, FTE edits — seven types) into the same log as bid changes with `'ALL'` in the user AND week slots; the renderer assumed every entry was a bid, so the filler values were printed as a person and fed to the week-date formatter. System entries now get their own sentence (`systemActionText`), their own SYSTEM pill, no week column, and a System option in the Action filter. Recognised by SHAPE as well as by the new `scope:'system'` stamp, so the rows already in the live log render correctly — no migration. **CL-2:** the same entries passed the bid-activity filters on BOTH pages: they inflated the admin Popcornometer and the bid chart's "N changes over Xh", and — user-facing — every bidder's "N changes in last 24h" chip and its popover on the staff page. Excluded on both, plus the staff 48h notification feed. NO rules change (the `changes` doc has no shape constraint for admin writes — checked). Twin change: admin + staff; CRNA re-stamped. Gates: NEW `tests/test-cl-system-entries.mjs` 34/34 — renders the owner's three real rows end to end through the REAL `renderChangelog` and asserts no "undefined", no "NaN", no "Wk 0", no "bid for" · honesty `--pre` vs the pushed 276/142 bytes fails 13 · FULL auction battery 21 suites / 1,350 green · schedule battery + isolation guard green · `node --check` clean on both pages. |
| **276** | `84f3b8a` · pushed 19 Aug | 19 Aug 2026 | TIMER RULES EDITOR — the TR bundle, from an owner bug report ("when I set the After x days feature, it doesn't update the correct line of the settings"). **TR-1:** the editor sorted the stages for DISPLAY and sorted them again on SAVE, so editing an "After N days" value moved that rule onto a line the admin had not typed on. `getTimerRules()` already sorts for the engine, so both were redundant — removed; a suite assertion pins that exactly ONE day-sort survives in the page. **TR-4 (owner's idea, same session):** edits are now BATCHED — a field change marks the editor dirty and re-checks the ladder, nothing is written until Save, and Save is still guarded by one confirm dialog (working rule 5). One dialog per edit session instead of one per field, and a multi-field edit is now ATOMIC (the old per-field save wrote half-finished ladders that were briefly the live rule). Cancel restores; `beforeunload` guards unsaved edits; the dirty flag blocks the snapshot-driven rebuild that could otherwise discard them. **TR-2:** the ladder is VALIDATED before it can be saved — switched-on steps must ascend in days and descend in hours; Save stays disabled with a plain-English reason. Previously a ladder that LENGTHENED mid-round saved silently and went out to every bidder in the welcome e-mail. **TR-3:** a line with no saved stage behind it now renders SWITCHED OFF instead of as a configured rule, so stages the admin never set can no longer be written live by editing an unrelated field. Admin page only (the staff page has no editor); CRNA re-stamped. Gates: NEW `tests/test-tr-timer-editor.mjs` 43/43 — executes the REAL render/read/validate/save functions against a DOM shim · honesty `--pre` vs the pushed 275 bytes fails 17 assertions including the line-jump and the invented-rule ones · FULL auction battery 20 suites / 1,316 green · schedule battery + isolation guard green · `node --check` clean on all four inline blocks. |
| **mobile 18** | `edb91f7` · pushed 18 Aug | 18 Aug 2026 | L5 CLOSED — the last July residual with a code fix (owner order: "let's finish the vacation builds entirely"). The redirect's reported leading-& mangle was already fixed in the shipped page; the remaining degenerate boundary — a flag VALUE merely starting with "1" (e.g. auto=12) clipped mid-value — closed by anchoring the strip regex `(?=&|$)`. Same build batch closes the RE-CHECKS: **L1 verified already fixed** in the shipped rules ([L-8] registered-user gate + [L1] append-only containment; console parity verified 18 Aug; forge-half stays the documented accepted residual) and **M1/L2/L3/L4 formally accepted for launch** (records only). NO rules change, NO other page bytes. Gates: NEW `tests/test-l1-l5-close.mjs` 17/17 — executes the REAL redirect script over every query shape and pins the L1 containment · honesty `--pre` vs a reconstructed mobile-17 fixture fails exactly on the boundary · FULL battery 19 suites / 1,273 green in-cloud. |
| **275 · staff 142** | `a4979a2` · pushed 18 Aug | 18 Aug 2026 | DASHBOARD HEADING DEDUPE + ALIGNMENT + V4 CLOSED (owner, on seeing 274 live: "i don't like this redundancy … probably just the bolded part is good"). The grey "Vacation auction — 2027" sub next to the dashboard title is REMOVED; its CONFIG-DERIVED year span (acYearHdr, set from auctionConfig) moves INTO the bold title, so "Dashboard MD Vacation Auction — 2027" now shows the config year rather than a hardcoded one (a down-payment on V4). Stamper rule updated to the span form; CRNA restamped (same dedupe there). Admin only; staff/crna-staff bytes unchanged. ALSO: the Timer Reset Mode card's three option rows LEFT-ALIGN (owner: "line things up better… timer mode 2 is the only 1 in the right position") — the .trow space-between was scattering them; radio at left, text hugging it, all three identical. **V4 (owner: "Add V4 to this"):** the explore proved B5 (136/265) already derives EVERYTHING from `auctionConfig.year` — both week lists (`getSundays(AC_YEAR)`), acYearHero/acYearSub (staff), acYearHdr/acYearLogin (admin); markup "2027"s are pre-load placeholders overwritten at boot. Real leftovers fixed: the admin title's hardcoded year (the dedupe moved the config span in) and the staff page's stale "Precompute…for 2027" comment (corrected → staff 142; CRNA staff restamped). Suite section executes getSundays with a NON-2027 year and pins every element's AC_YEAR wiring. V6 stays deferred (owner: "Leave V6 for later"). Gates: test-c7-timer-bundle now 65/65 · test-crna-stamp re-anchored · FULL battery 18 suites / 1,256 green · `node --check` clean. |
| **274 · staff 141** | `f40f592` · pushed 18 Aug (one commit with 273) | 18 Aug 2026 | THE C7 BUNDLE + V3 + V5 (owner order: "Then do C7 bundle and V3 as next build"; V5 included by owner choice — DECISIONS §59). **C7-LABELS:** MD labels on both pages (staff header "MD" sub + MD titles; admin "Dashboard MD Vacation Auction — 2027" + MD title) mirroring the CRNA labels; `crna-stamp.mjs` label rules now SWAP MD→CRNA instead of inserting into unlabeled pages. **C7-ROSTER:** the hardcoded starting roster removed from BOTH MD pages (`let names=[]`) — the database is the only source; test-crna-stamp's "control" re-anchored to pin the stronger truth. **V1:** allowance unlimited in EVERY phase collapses the priority-lock rule to the owner's exact wording (rules + welcome, both sites, twin-identical; mixed configs keep the breakdown). **V2:** the overpaying tip loses its "To maximize your own benefit," prefix in all three places. **V5:** Fair Play flags LOWERINGS from a winning position — same reconstruction as the cancel flag, own label/color, counts in totals, works in every allowance mode including unlimited (which was previously blind to lowering behaviour). **V3:** TIMER RESET MODE card — 1 classic (default, unchanged until deliberately chosen) / 2 affects-others only / 3 new-bids-always+affecting-changes; confirmed + audited (admin log) single-key merge write; the affects-others predicate is the outbid pipeline's own comparison, judging the SAME after-snapshot as the notifications (built once in persistScheduleChange); empty-week resolved with NO special case (§59); all guards (same-value, opening window, extend-only, expired-timer M-6) stand above the mode; staff Timer Rules text adapts per mode (mode 1 byte-compatible); simulator batch reset unchanged. CRNA restamped. Gates: NEW `tests/test-c7-timer-bundle.mjs` 53/53 executing the real functions · honesty `--pre` vs pushed 273/140 fixtures (md5s in header) fails · FULL battery 18 suites / 1,244 green in-cloud · `node --check` clean ×4 · prioLockBullets/blAllowance twins verified byte-identical. |
| **273** | `f40f592` · pushed 18 Aug (one commit with 274) | 18 Aug 2026 | V7c — THE NEXT-DECISION BUTTON GOES STICKY, IN THE CARD HEADER (owner feedback on 272, same day, verbatim in TODO: the 272 in-row button auto-jumped to the next decision the moment one was made, so it could never be clicked to scroll — "which is the entire point"). New model, owner's words: *"it should remain at the top most decision until it is clicked by admin. once it's clicked, the page should scroll and the button should appear by the next decision. if admin scrolls and the next decision button leaves the view, it should then auto-relocate to the next decision."* Shipped as: both card templates carry `data-card`; ONE "⏭ Next Decision" button in the card HEADER of the topmost pending draw/review, immediately RIGHT of "📋 Prior Phase Results" (untouched, per owner: "must stay where it is"). STICKY: never moves on a decision; moves ONLY on click (scroll+highlight the topmost pending, re-anchor to its card) or when it scrolls out of the viewport (quiet re-anchor to the first pending below the reader — never scrolls the page itself; 1.2s stand-down after a click so it can't fight the smooth scroll). Removed entirely when no draw/review is pending. Admin only; staff stays 140. CRNA restamped. Gates: `test-capacity-float.mjs` reworked to V7c, 47/47 · honesty `--pre` vs committed-272 fixture (md5 in header) fails · FULL battery 17 suites / 1,191 green in-cloud · `node --check` clean. |
| **272** | `86e6ca1` · pushed 18 Aug, live briefly — superseded same day by 273 | 18 Aug 2026 | V7b — THE NEXT-DECISION BUTTON GOES INLINE (owner feedback on 271, same day, verbatim in TODO: *"Instead of floating at the bottom, I want it to float immediately to the right of next decision from the top that needs to be made… IT SHOULD ONLY BE PRESENT next to draws/reviews… There should only be 1 floating button at a time… The key is to facilitate rapid decision making."*). 271's bottom-float (`_initJumpFloat`) fully REMOVED. New: rows carry `data-outcome`; `_placeJumpInline` puts ONE "⏭ Next Decision" button inside the actions cell of the TOPMOST undecided draw/review row, right of Approve/Deny; re-anchored on every adBody render, so deciding a row walks the button down the page by itself; removed entirely (not hidden) when no draw/review is pending. Click ADVANCES: next pending draw/review centers with the standard highlight; wraps from the last; a lone final decision toasts instead. Toolbar "Jump to Next Decision" unchanged (covers win/lose stragglers). Admin only; staff stays 140. CRNA restamped. Gates: `test-capacity-float.mjs` reworked to V7b, 44/44 · honesty `--pre` vs pushed 271 fixture (md5 in header) fails on identity + V7b exactly · FULL battery 17 suites / 1,188 green in-cloud · `node --check` clean. |
| **271** | `b088c66` · pushed 18 Aug | 18 Aug 2026 | B4 + V7 (admin only; staff stays 140). **B4 — AVAILABLE CAPACITY BY WEEK** (owner request 17 Aug, TODO §1 B4): new report in the dashboard's Summaries card, beneath the User summary section. One row per Monday, whole year: capacity · taken · available · approved winners (with FTE). `_capacityWeekRows` counts APPROVED WINNERS ONLY — completed-phase approvals + announced P4 round wins + current-phase admin approvals, de-duped, valued at live FTE; NEVER live bids, NEVER projections (deliberately not `finalFteForWeek`). The definition rides the card, the report subtitle AND the xlsx band (owner: "specify that in the header"): "Available = capacity minus approved winners; pending bids not counted". View / PDF / Excel, same styling + pop-up CSS as the other reports; negative available shown, not clamped; unsaved weeks stay capacity 0. **V7 — THE JUMP BUTTON FLOATS** (owner spec 18 Aug, verbatim in TODO): `_initJumpFloat` shows a fixed copy of "⏭ Jump to Next Decision" at the bottom of the window, at the toolbar original's left-to-right position (clamped to the viewport), whenever the original is scrolled out of view or under the sticky header (same 120px margin the jump uses); solid-backed + shadowed; hides with the panel; same handler, no new state; z-index 80 = above content, below every modal/toast. CRNA restamped (test-crna-stamp pins parity). Gates: NEW `tests/test-capacity-float.mjs` 40/40 executing the real extracted functions (capacity math incl. de-dupe/negative/no-default-capacity, all three export formats, float show/hide/clamp/click) · honesty `--pre` vs pushed 270 fixture (md5 in the suite header) fails · FULL battery 17 suites / 1,184 green in-cloud · `node --check` clean. Both new suites' build-identity pins made forward-compatible (≥ floor, not ===) after the battery caught the M3 suite pinning ===270. |
| **270 · staff 140** | `9f49604` · pushed 18 Aug | 18 Aug 2026 | THE M3 BUILD — mixed-case e-mail fix (TODO §1 B2, scope frozen 17 Aug + one 18 Aug ruling, DECISIONS §58). (1) READ SIDE: admin `recipientsFor` returns LOWER-CASED addresses (`out.push(k)`) — what its comment always claimed and what the staff site always did — so the two sites agree regardless of stored case; no data migration. (2) SAVE HYGIENE, both sites' KP-address saves (`_normKpEmail`, byte-identical in both files): trim + lower-case; refuse ONLY the unarguable — whitespace inside · more than one @ · domain without a dot · leading/trailing dot in local part or domain. NO domain policy (HANDOFF D5). Admin Users-page save, Add User secondary (refusal now toasted, never silent), staff KP prompt. (3) CASE-BLIND ALREADY-SENT CHECKS (owner ruling 18 Aug): `_sentAddrsFor` + `_ledgerFresh` lower-case ledger membership; both sites' mail-queue `sentTo` skips lower-case before comparing — a pre-270 mixed-case ledger entry can never cause a duplicate results e-mail on retry. CRNA restamped from the fixed MD pages (crna/ bytes change; mechanical, pinned by test-crna-stamp). Gates: NEW `tests/test-m3-email-case.mjs` 43/43 executing both sites' real extracted functions on a mixed-case roster · honesty `--pre` vs pushed 269/139 fixtures (md5s in the suite header) fails exactly on the defect · FULL auction battery 16 suites / 1,144 assertions green in-cloud · `node --check` clean on all 4 pages. Two suite harnesses re-anchored to the lower-case contract (test-p4-rounds stub + [D] fixture kept mixed-case on purpose; test-audit-fixes D8 sandbox bundles `_normKpEmail`) — no assertion weakened. |
