# BUILD LOG — Vacation Auction (MD)

**Why this file exists.** Same reason as the schedule's: commit subjects have been lost to
mis-pastes before, so every build gets a row here, written at the same time as the code.
Agreed 17 Aug 2026; starts at build 270 (history before 270 lives in
`anesthesia-kp.github.io/HANDOFF.md` and `DECISIONS.md`, both committed).

The build number below is authoritative: read out of `var BUILD` in that commit's own
bytes, not taken from the commit subject.

| build | commit | date | what shipped |
|---|---|---|---|
| **277 · staff 143** | _pending push_ | 19 Aug 2026 | CHANGE LOG — system admin actions stop pretending to be bids. From an owner screenshot of three rows reading "Admin admin-timer-mode-1 bid for ALL on Wk 0 · undefined undefined NaN". **CL-1:** `adminLog` writes system-level actions (timer mode, restores, user removal, FTE edits — seven types) into the same log as bid changes with `'ALL'` in the user AND week slots; the renderer assumed every entry was a bid, so the filler values were printed as a person and fed to the week-date formatter. System entries now get their own sentence (`systemActionText`), their own SYSTEM pill, no week column, and a System option in the Action filter. Recognised by SHAPE as well as by the new `scope:'system'` stamp, so the rows already in the live log render correctly — no migration. **CL-2:** the same entries passed the bid-activity filters on BOTH pages: they inflated the admin Popcornometer and the bid chart's "N changes over Xh", and — user-facing — every bidder's "N changes in last 24h" chip and its popover on the staff page. Excluded on both, plus the staff 48h notification feed. NO rules change (the `changes` doc has no shape constraint for admin writes — checked). Twin change: admin + staff; CRNA re-stamped. Build 276 was pushed before this one was filed, so 277 is its own commit. Gates: NEW `tests/test-cl-system-entries.mjs` 34/34 — renders the owner's three real rows end to end through the REAL `renderChangelog` and asserts no "undefined", no "NaN", no "Wk 0", no "bid for" · honesty `--pre` vs the pushed 276/142 bytes fails 13 · FULL auction battery 21 suites / 1,350 green · schedule battery + isolation guard green · `node --check` clean on both pages. |
| **276** | `84f3b8a` · pushed 19 Aug | 19 Aug 2026 | TIMER RULES EDITOR — the TR bundle, from an owner bug report ("when I set the After x days feature, it doesn't update the correct line of the settings"). **TR-1:** the editor sorted the stages for DISPLAY and sorted them again on SAVE, so editing an "After N days" value moved that rule onto a line the admin had not typed on. `getTimerRules()` already sorts for the engine, so both were redundant — removed; a suite assertion pins that exactly ONE day-sort survives in the page. **TR-4 (owner's idea, same session):** edits are now BATCHED — a field change marks the editor dirty and re-checks the ladder, nothing is written until Save, and Save is still guarded by one confirm dialog (working rule 5). One dialog per edit session instead of one per field, and a multi-field edit is now ATOMIC (the old per-field save wrote half-finished ladders that were briefly the live rule). Cancel restores; `beforeunload` guards unsaved edits; the dirty flag blocks the snapshot-driven rebuild that could otherwise discard them. **TR-2:** the ladder is VALIDATED before it can be saved — switched-on steps must ascend in days and descend in hours; Save stays disabled with a plain-English reason. Previously a ladder that LENGTHENED mid-round saved silently and went out to every bidder in the welcome e-mail. **TR-3:** a line with no saved stage behind it now renders SWITCHED OFF instead of as a configured rule, so stages the admin never set can no longer be written live by editing an unrelated field. Admin page only (the staff page has no editor); CRNA re-stamped. Gates: NEW `tests/test-tr-timer-editor.mjs` 43/43 — executes the REAL render/read/validate/save functions against a DOM shim · honesty `--pre` vs the pushed 275 bytes fails 17 assertions including the line-jump and the invented-rule ones · FULL auction battery 20 suites / 1,316 green · schedule battery + isolation guard green · `node --check` clean on all four inline blocks. |
| **mobile 18** | `edb91f7` · pushed 18 Aug | 18 Aug 2026 | L5 CLOSED — the last July residual with a code fix (owner order: "let's finish the vacation builds entirely"). The redirect's reported leading-& mangle was already fixed in the shipped page; the remaining degenerate boundary — a flag VALUE merely starting with "1" (e.g. auto=12) clipped mid-value — closed by anchoring the strip regex `(?=&|$)`. Same build batch closes the RE-CHECKS: **L1 verified already fixed** in the shipped rules ([L-8] registered-user gate + [L1] append-only containment; console parity verified 18 Aug; forge-half stays the documented accepted residual) and **M1/L2/L3/L4 formally accepted for launch** (records only). NO rules change, NO other page bytes. Gates: NEW `tests/test-l1-l5-close.mjs` 17/17 — executes the REAL redirect script over every query shape and pins the L1 containment · honesty `--pre` vs a reconstructed mobile-17 fixture fails exactly on the boundary · FULL battery 19 suites / 1,273 green in-cloud. |
| **275 · staff 142** | `a4979a2` · pushed 18 Aug | 18 Aug 2026 | DASHBOARD HEADING DEDUPE + ALIGNMENT + V4 CLOSED (owner, on seeing 274 live: "i don't like this redundancy … probably just the bolded part is good"). The grey "Vacation auction — 2027" sub next to the dashboard title is REMOVED; its CONFIG-DERIVED year span (acYearHdr, set from auctionConfig) moves INTO the bold title, so "Dashboard MD Vacation Auction — 2027" now shows the config year rather than a hardcoded one (a down-payment on V4). Stamper rule updated to the span form; CRNA restamped (same dedupe there). Admin only; staff/crna-staff bytes unchanged. ALSO: the Timer Reset Mode card's three option rows LEFT-ALIGN (owner: "line things up better… timer mode 2 is the only 1 in the right position") — the .trow space-between was scattering them; radio at left, text hugging it, all three identical. **V4 (owner: "Add V4 to this"):** the explore proved B5 (136/265) already derives EVERYTHING from `auctionConfig.year` — both week lists (`getSundays(AC_YEAR)`), acYearHero/acYearSub (staff), acYearHdr/acYearLogin (admin); markup "2027"s are pre-load placeholders overwritten at boot. Real leftovers fixed: the admin title's hardcoded year (the dedupe moved the config span in) and the staff page's stale "Precompute…for 2027" comment (corrected → staff 142; CRNA staff restamped). Suite section executes getSundays with a NON-2027 year and pins every element's AC_YEAR wiring. V6 stays deferred (owner: "Leave V6 for later"). Gates: test-c7-timer-bundle now 65/65 · test-crna-stamp re-anchored · FULL battery 18 suites / 1,256 green · `node --check` clean. |
| **274 · staff 141** | `f40f592` · pushed 18 Aug (one commit with 273) | 18 Aug 2026 | THE C7 BUNDLE + V3 + V5 (owner order: "Then do C7 bundle and V3 as next build"; V5 included by owner choice — DECISIONS §59). **C7-LABELS:** MD labels on both pages (staff header "MD" sub + MD titles; admin "Dashboard MD Vacation Auction — 2027" + MD title) mirroring the CRNA labels; `crna-stamp.mjs` label rules now SWAP MD→CRNA instead of inserting into unlabeled pages. **C7-ROSTER:** the hardcoded starting roster removed from BOTH MD pages (`let names=[]`) — the database is the only source; test-crna-stamp's "control" re-anchored to pin the stronger truth. **V1:** allowance unlimited in EVERY phase collapses the priority-lock rule to the owner's exact wording (rules + welcome, both sites, twin-identical; mixed configs keep the breakdown). **V2:** the overpaying tip loses its "To maximize your own benefit," prefix in all three places. **V5:** Fair Play flags LOWERINGS from a winning position — same reconstruction as the cancel flag, own label/color, counts in totals, works in every allowance mode including unlimited (which was previously blind to lowering behaviour). **V3:** TIMER RESET MODE card — 1 classic (default, unchanged until deliberately chosen) / 2 affects-others only / 3 new-bids-always+affecting-changes; confirmed + audited (admin log) single-key merge write; the affects-others predicate is the outbid pipeline's own comparison, judging the SAME after-snapshot as the notifications (built once in persistScheduleChange); empty-week resolved with NO special case (§59); all guards (same-value, opening window, extend-only, expired-timer M-6) stand above the mode; staff Timer Rules text adapts per mode (mode 1 byte-compatible); simulator batch reset unchanged. CRNA restamped. Gates: NEW `tests/test-c7-timer-bundle.mjs` 53/53 executing the real functions · honesty `--pre` vs pushed 273/140 fixtures (md5s in header) fails · FULL battery 18 suites / 1,244 green in-cloud · `node --check` clean ×4 · prioLockBullets/blAllowance twins verified byte-identical. |
| **273** | `f40f592` · pushed 18 Aug (one commit with 274) | 18 Aug 2026 | V7c — THE NEXT-DECISION BUTTON GOES STICKY, IN THE CARD HEADER (owner feedback on 272, same day, verbatim in TODO: the 272 in-row button auto-jumped to the next decision the moment one was made, so it could never be clicked to scroll — "which is the entire point"). New model, owner's words: *"it should remain at the top most decision until it is clicked by admin. once it's clicked, the page should scroll and the button should appear by the next decision. if admin scrolls and the next decision button leaves the view, it should then auto-relocate to the next decision."* Shipped as: both card templates carry `data-card`; ONE "⏭ Next Decision" button in the card HEADER of the topmost pending draw/review, immediately RIGHT of "📋 Prior Phase Results" (untouched, per owner: "must stay where it is"). STICKY: never moves on a decision; moves ONLY on click (scroll+highlight the topmost pending, re-anchor to its card) or when it scrolls out of the viewport (quiet re-anchor to the first pending below the reader — never scrolls the page itself; 1.2s stand-down after a click so it can't fight the smooth scroll). Removed entirely when no draw/review is pending. Admin only; staff stays 140. CRNA restamped. Gates: `test-capacity-float.mjs` reworked to V7c, 47/47 · honesty `--pre` vs committed-272 fixture (md5 in header) fails · FULL battery 17 suites / 1,191 green in-cloud · `node --check` clean. |
| **272** | `86e6ca1` · pushed 18 Aug, live briefly — superseded same day by 273 | 18 Aug 2026 | V7b — THE NEXT-DECISION BUTTON GOES INLINE (owner feedback on 271, same day, verbatim in TODO: *"Instead of floating at the bottom, I want it to float immediately to the right of next decision from the top that needs to be made… IT SHOULD ONLY BE PRESENT next to draws/reviews… There should only be 1 floating button at a time… The key is to facilitate rapid decision making."*). 271's bottom-float (`_initJumpFloat`) fully REMOVED. New: rows carry `data-outcome`; `_placeJumpInline` puts ONE "⏭ Next Decision" button inside the actions cell of the TOPMOST undecided draw/review row, right of Approve/Deny; re-anchored on every adBody render, so deciding a row walks the button down the page by itself; removed entirely (not hidden) when no draw/review is pending. Click ADVANCES: next pending draw/review centers with the standard highlight; wraps from the last; a lone final decision toasts instead. Toolbar "Jump to Next Decision" unchanged (covers win/lose stragglers). Admin only; staff stays 140. CRNA restamped. Gates: `test-capacity-float.mjs` reworked to V7b, 44/44 · honesty `--pre` vs pushed 271 fixture (md5 in header) fails on identity + V7b exactly · FULL battery 17 suites / 1,188 green in-cloud · `node --check` clean. |
| **271** | `b088c66` · pushed 18 Aug | 18 Aug 2026 | B4 + V7 (admin only; staff stays 140). **B4 — AVAILABLE CAPACITY BY WEEK** (owner request 17 Aug, TODO §1 B4): new report in the dashboard's Summaries card, beneath the User summary section. One row per Monday, whole year: capacity · taken · available · approved winners (with FTE). `_capacityWeekRows` counts APPROVED WINNERS ONLY — completed-phase approvals + announced P4 round wins + current-phase admin approvals, de-duped, valued at live FTE; NEVER live bids, NEVER projections (deliberately not `finalFteForWeek`). The definition rides the card, the report subtitle AND the xlsx band (owner: "specify that in the header"): "Available = capacity minus approved winners; pending bids not counted". View / PDF / Excel, same styling + pop-up CSS as the other reports; negative available shown, not clamped; unsaved weeks stay capacity 0. **V7 — THE JUMP BUTTON FLOATS** (owner spec 18 Aug, verbatim in TODO): `_initJumpFloat` shows a fixed copy of "⏭ Jump to Next Decision" at the bottom of the window, at the toolbar original's left-to-right position (clamped to the viewport), whenever the original is scrolled out of view or under the sticky header (same 120px margin the jump uses); solid-backed + shadowed; hides with the panel; same handler, no new state; z-index 80 = above content, below every modal/toast. CRNA restamped (test-crna-stamp pins parity). Gates: NEW `tests/test-capacity-float.mjs` 40/40 executing the real extracted functions (capacity math incl. de-dupe/negative/no-default-capacity, all three export formats, float show/hide/clamp/click) · honesty `--pre` vs pushed 270 fixture (md5 in the suite header) fails · FULL battery 17 suites / 1,184 green in-cloud · `node --check` clean. Both new suites' build-identity pins made forward-compatible (≥ floor, not ===) after the battery caught the M3 suite pinning ===270. |
| **270 · staff 140** | `9f49604` · pushed 18 Aug | 18 Aug 2026 | THE M3 BUILD — mixed-case e-mail fix (TODO §1 B2, scope frozen 17 Aug + one 18 Aug ruling, DECISIONS §58). (1) READ SIDE: admin `recipientsFor` returns LOWER-CASED addresses (`out.push(k)`) — what its comment always claimed and what the staff site always did — so the two sites agree regardless of stored case; no data migration. (2) SAVE HYGIENE, both sites' KP-address saves (`_normKpEmail`, byte-identical in both files): trim + lower-case; refuse ONLY the unarguable — whitespace inside · more than one @ · domain without a dot · leading/trailing dot in local part or domain. NO domain policy (HANDOFF D5). Admin Users-page save, Add User secondary (refusal now toasted, never silent), staff KP prompt. (3) CASE-BLIND ALREADY-SENT CHECKS (owner ruling 18 Aug): `_sentAddrsFor` + `_ledgerFresh` lower-case ledger membership; both sites' mail-queue `sentTo` skips lower-case before comparing — a pre-270 mixed-case ledger entry can never cause a duplicate results e-mail on retry. CRNA restamped from the fixed MD pages (crna/ bytes change; mechanical, pinned by test-crna-stamp). Gates: NEW `tests/test-m3-email-case.mjs` 43/43 executing both sites' real extracted functions on a mixed-case roster · honesty `--pre` vs pushed 269/139 fixtures (md5s in the suite header) fails exactly on the defect · FULL auction battery 16 suites / 1,144 assertions green in-cloud · `node --check` clean on all 4 pages. Two suite harnesses re-anchored to the lower-case contract (test-p4-rounds stub + [D] fixture kept mixed-case on purpose; test-audit-fixes D8 sandbox bundles `_normKpEmail`) — no assertion weakened. |
